# EOP-VS-BPMN-ISA-002 — Structured-Concurrency Vocabulary for the bpmn-lite Fibre Machine (ISA v2)

**Status:** v0.17 — **IMPLEMENTED** (§30 marker + consolidated deviations table; open gate: 6.4 destructive cutover). **§31 (v0.13, 2026-07-24) closes V6.6: the guard-failure budget ceiling is per-guard and artifact-resident (`v2_guard_budgets` side table), retiring the hardcoded `ScopeFailureBudget(1,5)`; counter stays store-side per §15.** **§26 (2026-07-24) closes a cutover-readiness audit of timeCycle XML lowering (`GUARD-TIMER-CYCLE>`): a plain non-cycle boundary timer (`timeDuration`, no `timeCycle`) rearmed indefinitely instead of firing once — `V2GuardArmTimer` defaulted every non-interrupting guard's repeat spec to `remaining: u32::MAX`, and only a following `GUARD-TIMER-CYCLE>` (present only for an authored cycle) ever narrowed it. Fixed by defaulting to `remaining: 1` instead, which has no effect on the cyclic case since `GUARD-TIMER-CYCLE>` already overwrites the count outright rather than narrowing it.** §25 (2026-07-24) closes a cutover-readiness audit of the BoundaryError v2 migration (`GUARD-ERROR>`): two `BoundaryError` nodes on the same host task with the SAME specific error code compiled and verified cleanly, but the runtime's `record.error_routes.find()` only ever matches the first-armed route — the second was silently unreachable, not rejected. Closed with a new IR-level check (`verifier.rs` 8e) and a matching bytecode-level check (`v2_verifier.rs`), both defense-in-depth pairs to the pre-existing catch-all-duplicate checks.** §24 (2026-07-24) closes Adam's follow-up question after §23 ("is the pending-aware fetch now the only way to read the concurrency table mid-transition?") **— no: a direct audit found two more live instances of the same hazard class (`v2_rollback_guard_scope`, `Instr::V2GuardArmTimer`'s record_kind lookup) plus a bulk-shaped sibling (`Instr::EndTerminate`'s orphaned-record sweep), all fixed, and a new CI lint added as the structural backstop per-site discipline had already proven insufficient once.** §23 (2026-07-24) closes §21's open design fork: `compute_region_map`/`compute_gateway_pairing` rebuilt on genuine post-dominance (Cooper/Harvey/Kennedy), replacing the in-degree heuristic and Direction A's own DFS-stack pairing mechanism with one shared structural computation; an independent review found and this landing fixed a real, previously-unenforced gap — no admission-time cycle check existed anywhere in the XML pipeline, and the dominance algorithm silently returns a wrong node (not a safe absence) on cyclic input — closed with a genuine `is_cyclic_directed` gate in `verifier.rs`, consistent with this document's own "SESE only, loops are finitely bounded" decisions.** §22 (2026-07-24) closes the K-1 finding from §20/§21: root cause was a stale-read-within-transition hazard in `apply_tick` (not the originally-hypothesized ancestor-membership deletion gap), fixed with a shared pending-aware record-fetch helper, both previously-`#[ignore]`d reproductions now pass 100/100 standalone runs.** §21 (2026-07-24) addresses §20 item 2 (basic AND-fork address-layout compile failure — fixed), re-runs and broadens §20 item 1's scope (K-1 now known to trigger on any nested-barrier `GatewayAnd`-in-`GatewayAnd` topology, not just inclusive-gateway dynamic arity), and reports a NEW, distinct, adjacent defect (region-merge misattribution to an unrelated in-degree>1 node, e.g. a boundary-error escalation landing inside a branch) as an open design fork — not decided, not fixed.** §20 (2026-07-24) records two open, pre-existing defects found while lifting the multiple-`GatewayInclusive`-pair admission gate (a K-1 runtime violation and a basic AND-fork compile failure) — reported, not fixed, not amendments.** §19 (2026-07-24) records Direction A — the compiler's fork/join pairing mechanism (`GatewayAnd`/`GatewayInclusive`) rewritten nesting-aware after a load-bearing mispairing defect was found in the BFS-order pairing this document's V-3 entry didn't anticipate could itself be wrong; V-3's own text is unchanged and was never wrong, only what supplied its input.** §18 lands one consolidating amendment covering everything ratified since v0.8 — A18's guard/rollback re-derivation (already implemented and committed; §18 is the paperwork catching up to it), Cluster B's dynamic-arity reversal (the v0.9 firm deferral is overturned — parallel multi-instance and inclusive gateways are IN for v2, not deferred to v3), Q6's ratification (guards carry an arming trigger; a boundary timer is a guard with a deadline), a new zero-match theorem and runtime rule (V-11, compile-time terminal-reachability; unmatched routes raise an incident, not a termination), and parallel multi-instance's own three deltas from Zeebe/Camunda semantics.** §10 has zero open reviewer questions again as of this amendment — the second time this document has said that (see §17's version of the same claim, which held for less than a day; this one is not expected to be more durable than the next implementation step that finds something). See §18 for full detail; nothing below is rewritten, only superseded in place where §18 says so. v0.9, plus one correction the same day — **§10's Q6 (opened 2026-07-22, during V5 boundary-timer lowering) is now the sole open reviewer question.** Materially different from Q5/Gap 1 below (scope deferrals): v1 executes boundary timers on synchronous tasks today and v2's word set has no equivalent (`V2RaceArm` has no job-completion arm; `V2Guard` carries no trigger mechanism of its own) — deferring would be a regression, not a scope-preserving choice. Held for ruling; a candidate (a guard-arming amendment — `GUARD>` gains a trigger spec, timer as the first kind) is under evaluation, not yet ratified. Full detail in `EOP-PLAN-BPMN-ISA-002.md`'s V5.1 boundary-timer entry. The rest of v0.9 (below) landed exactly as described and remains accurate: Q1 discharged by the V4.6 fuzz corpus + Ring 3 evidence (not a ratified dialogue, recorded as a different discharge mechanism); Q3 settled by what Ring 1 actually shipped (whole-frame BLAKE3), its projection sub-question left open; Q5 merged with the inclusive-gateway/parallel-MI Gap 1 into one question after a mid-session correction — a ruling that inclusive gateways had "zero live usage" was itself wrong (the search never checked `bpmn-lite-engine/src/tests.rs`'s six-test `T-IG` suite), caught by a full-workspace test run and reverted before landing, corrected to a shared v3 `FORK-DYN` deferral rather than rejection — then that merged question was itself put to Adam directly and ratified closed same-day: dynamic-arity concurrency is **not required in this V&S**, both defer to v3 as a firm (not conditional) deferral; §8's word count corrected from an approximate "+~14" to the exact "+16"; §9's DSL-lowering input marked SATISFIED; `opened_at`'s status (v0.7/v0.8) confirmed unaffected by §15's instance-granular-refusal ruling. See §17 for the full accounting; the inclusive-gateway episode's process detail lives in the plan doc and V0 report, not repeated here. v0.8 revised v0.7 per §16: landing ruling F required `ConcurrencyRecord::opened_at: Option<Addr>`, a frame-resident artifact address, contradicting §15's own claim that F needed no §4 interaction. Checking that surfaced §4's text was already inaccurate — `handler: Option<Addr>` had carried an artifact address since V1.2 without ever violating a K-invariant. §4 is revised to state the real rule: three categories of `Addr` presence in runtime state (code reference/jump target — legitimate; runtime execution identity — forbidden, FORK/JOIN static pairing is the named exemplar; static-site identity for cross-activation accounting — legitimate, added by this amendment). `handler` is category 1, `opened_at` is category 3; no carve-out was needed once §4 said what it meant. §15's §4-interaction paragraph is corrected in place to match. A second, related correction lands with it: §15's "never in the hash domain" principle is clarified — it governs the counter's ticking, not the terminal value recorded once as a field on a genuine execution object (`Incident::retry_count`), which was already part of Ring 2's canonical encoding before ruling E and is not a contradiction. See §16, which also records this as the third instance of the same pattern (a correct practice, an inaccurate slogan) and the authoring habit adopted to catch it earlier next time. v0.7 revised v0.6 per Adam's ratified rulings E/F (§15): the two implementation gaps §14 deliberately left open are closed. E — the attempt-history channel into `Incident` is `Command::EffectFailed`/`EffectResponse::Failed` gaining an `attempt: u32` field, populated by `bpmn-lite-engine`'s `schedule_transient_effect` (the sole site that already calls `RetryPolicy::decision()`), not by individual worker call sites. F — the repeated-failure escalation counter for `ContractViolation` lives store-side, keyed `(tenant_id, instance_id, guard_addr)`, checked and enforced at claim time (T10.3's existing "should this instance run" gate), not in the kernel frame and not as a merely-advisory event consumer. Both settle a unifying principle: execution state lives in the hashed frame; operational accounting about execution lives beside it, claim-gated and journaled, never in the hash domain. See §15 for the full amendment, including the rejected/superseded options and the §4 interaction analysis. v0.6 revised v0.5 per Adam's ratified ruling D (§14): §13 ruling C's "no distinction between error classes" clause is superseded — automatic rollback is now scoped to `ContractViolation` only, with `BusinessRejection`(unmatched) and exhausted-`Transient` always surfacing as an `Incident` instead. Found during V4.6's blind review of ruling C's actual consequences, then corrected directly by Adam. v0.5 revised v0.4 per Adam's ratified rulings, worked out through direct dialogue while building V4.1's kernel words, on `GUARD-N>` re-arm (Q2), `CANCEL-SCOPE`'s compensation-op reuse (Q4), and automatic rollback on definitive failure inside an interrupting guard (a new question §10 hadn't anticipated). See §13 for that amendment note. v0.4 revised v0.3 per Adam's ratified rulings on three points drafting `EOP-EX-BPMN-ISA-002` (the oracle) surfaced as underdetermined: cancellation order (§4, §12), `JOIN` survivor semantics (§5, §12), and control-stack-delta emission for deleted fibres (§4, §12). See §12 for that amendment note. v0.3 revised v0.2 per adversarial review R1 (verdict: proceed; all points dispositioned in §11); v0.3 adds the static-structure/dynamic-activation law (§4) and converts `JOIN` to handle-based resolution (§5, V-3) after a code/data-separation challenge caught static-id lookup as a taxonomy leak.
**Depends on:** EOP-PLAN-BPMN-KERNEL-001 landed in full (T4–T12); artifact/snapshot versioning (E8), replay harness (T10), differential-execution harness (T9.7) all operational.
**Deployment context:** greenfield — no production instances exist. Backward compatibility is a non-goal; persisted-state versioning is retained solely as a corruption tripwire (see D3).
**Companion documents (to follow):** EOP-EX-BPMN-ISA-002 (worked example / shared oracle), EOP-PLAN-BPMN-ISA-002 (tranche plan V1–V6).
**Review protocol:** authorship-blind; reviewer context assembled by inclusion-list; the worked example is oracle-excluded until D1–D4 ratify.

---

> ## Design principle
> **Side tables are code. Fibre state is data.**
> Instructions manipulate fibre-owned concurrency state. Everything the machine consults at run time to decide concurrency behaviour is data written by words inside `kernel::apply`, sealed under the frame hash, and replayed deterministically. Everything static is dictionary: hashed in the artifact envelope, immutable, never consulted as runtime state. Every decision in this document is judged against this principle.

## 1. Context and problem statement

Post-KERNEL-001, the fibre machine is durably correct: one deterministic `kernel::apply`, one fenced `commit_transition`, fibres as persisted continuations (PC + operand stack + registers + wait state). What remains structurally unsatisfying is that BPMN's *event-driven and structured-concurrency* semantics — join barriers, race arming/resolution, boundary-event scopes, timer arming — are encoded outside the instruction stream: partly in artifact side tables (race plans, join metadata, boundary routes), partly in auxiliary runtime rows, and historically in engine post-tick passes.

KERNEL-001 T7 made these paths *atomic* by folding them into `apply`, but it did not make them *instructional*. The kernel still interprets side-table cross-references at run time to decide concurrency behaviour. Consequences:

1. **Verification is weaker than it could be.** The T6.3 verifier proves operand-stack safety per CFG edge, but concurrency well-formedness (guard balance, race nesting, fork/join arity) is only checkable by cross-referencing side tables against the instruction stream — referential integrity, not dataflow proof.
2. **Executable meaning is split between code and metadata.** Roughly half of BPMN semantics lives in side tables. This forced the T6 envelope to hash everything, which is correct but treats a symptom.
3. **The kernel carries interpretation logic that a richer ISA would delete.** Race resolution and boundary promotion are ~semantic switch statements over metadata rather than word execution.

### Why FORTH is the right analogy

BPMN-lite execution is already a stack machine: a fibre is a persisted continuation whose entire execution state is a program counter, an operand stack, and registers — precisely the FORTH inner-interpreter shape, chosen originally because it is the minimal machine whose complete state can be serialized at any instruction boundary. FORTH is therefore not a stylistic reference but the closest prior art for the specific problem this ISA solves: durable, resumable execution where the machine state *is* the context-switch frame. FORTH's two-stack architecture (parameter stack for data, return stack for control) is the template this proposal completes: v1 built the parameter stack; v2 adds the control stack. One deliberate departure from a classic threaded interpreter: persistence granularity is the **transition**, not the instruction. A classic FORTH thread may stop between any two NEXTs; this machine chooses its stopping points — parks and commits — and everything between two sealed frames is a pure, deterministic, replayable burst. Commit frequency is therefore proportional to *waits*, not to instruction count.

### Structured-concurrency lineage

The proposal imports one older idea into BPMN execution: control structures should be block-structured and statically checkable. Dijkstra's argument against unstructured jumps, Hoare's CSP alternation (the direct ancestor of `RACE{`), and the modern structured-concurrency principle — a concurrent child's lifetime is bounded by a syntactic scope in its parent — are the intellectual landmarks. BPMN's boundary events, event gateways, and parallel joins are structured-concurrency constructs that the v1 encoding flattened into metadata; v2 restores their block structure to the instruction stream, where a verifier can prove it.

## 2. Glossary (binding for all ISA-002 documents and generated identifiers)

| Term | Meaning — and only this meaning |
|---|---|
| **Scope** | A dynamic extent opened by a word (`GUARD>`, `RACE{`) and closed by its pair, within which fibres are members. The general category. |
| **Guard** | A scope of kind *guard*: carries a handler; interrupting (`GUARD>`) unwinds members on trigger, non-interrupting (`GUARD-N>`) spawns the handler without unwinding. |
| **Race** | A scope of kind *race*: first-wins over N armed alternatives; resolution runs the winner and cancels losing members in one transition. |
| **Boundary event** | The *BPMN authoring construct* that frontends lower to a guard. Never used to name runtime state. |
| **Barrier** | Synchronisation state: the arrival counter created by `FORK n` and consumed by `JOIN n id`. Not a scope. |
| **Control stack** | The per-fibre second stack (return-stack analogue): an ordered stack of concurrency-record handles the fibre is inside. Holds scope handles today; compensation and transaction records later. |
| **Concurrency table** | The snapshot-resident table of concurrency records — scopes, barriers, (later) compensation records — keyed by record ID: kind, members, handler, state, counters. |
| **Handle** | A fibre's reference to a concurrency-table record (an ID pushed on its control stack). |
| **Frame** | The complete persisted context-switch unit hashed by D3-L2: snapshot ‖ fibres ‖ concurrency table ‖ pending effects ‖ revision ‖ artifact hash. Frame size is proportional to live tokens and their bounded stacks — never to program size; the program is shared, immutable, and referenced by hash only. |
| **Instruction (cell)** | One element of the compiled, flattened program — the threaded-code "necklace." Static, immutable, addressed by `Addr`. Never called a token. |
| **Token** | The BPMN sense only: a locus of execution moving through the program. Realized as a **fibre** — a cursor (PC) into the instruction list plus its operand stack, control stack, and registers. The AST/DAG is compile-time only; tokens traverse compiled cells, never tree nodes. |

## 3. Non-goals

- No change to the durability substrate: `Claim`, `Transition`, `commit_transition`, fencing, effect identity, journal, and replay are consumed as-is.
- No change to the effect boundary with ob-poc/DSL runtime: verbs remain `DurableEffect::Invoke` over the bus. This V&S is about *process time*, not domain meaning.
- No new BPMN feature coverage beyond what v1 executes today, except where D2 explicitly admits it (non-interrupting guards).
- No dual-ISA kernel and no migration machinery of any kind: greenfield deployment, exactly one instruction set live in `apply`, cutover is wipe-and-recompile (see D3).
- No error correction or self-healing. The integrity model is detect-and-fail-stop only.

## 4. Decision D1 — Concurrency-state ownership across fibres

### The problem

FORTH `CATCH`/`THROW` marks the return stack of one thread of execution. A BPMN guard covers a *subtree of fibres*: an interrupting timer on a subprocess must, on firing, unwind and cancel every token inside that subprocess, including tokens forked after the guard was armed. A race is first-wins over N alternatives where losers' members are cancelled *in the same transition* as the winner proceeds. A per-fibre stack alone cannot express "this scope covers fibres that do not exist yet."

### Option A — Record ownership by arming fibre, membership by inherited lineage path

- Pros: most FORTH-native; mutation fibre-local; records serialize inside the owning fibre.
- Cons: cancellation scans all fibre paths; records outliving their arming fibre (arming fibre parks/ends while children run) require an ownership-transfer rule — the design wart.

### Option B — Concurrency table alone (fibres reference records by ID, no per-fibre stack)

- Pros: cancellation set directly enumerable; record lifetime decoupled from any fibre.
- Cons: the verifier loses its literal stack; per-fibre balance becomes a derived property proven over a synthesized list — strictly weaker dataflow.

### Option C — Control stack of handles + concurrency table (recommended)

Fibres carry a **control stack** of handles (literal push/pop, classic dataflow for the verifier). The **concurrency table** holds the records: kind, membership, handler, state, counters. `GUARD>` allocates a record and pushes its handle; `<GUARD` pops and retires it; `FORK` copies the parent's control stack into children and registers them as members; `JOIN` decrements a barrier record.

**Canonicity (revised per review R1):** membership is *not* derivable from the control stacks — once cancellation and fibre death exist, a dead fibre's stack is gone while the record's membership history may still matter (and will, for compensation). Therefore:

> The control stack is canonical. Concurrency-table membership is canonical. Their mutual consistency is an invariant (K-2, §7) maintained exclusively by word execution inside `apply`, shadow-asserted at every park/resume (D3-L4).

This also sharpens the response to the "A and B welded together" rebuttal: C is not one fact in two representations; it is two canonical facts — *what am I inside?* (control stack) and *who is inside me?* (membership) — bound by a consistency law. The two questions are fundamentally different; no single structure answers both without pathology.

**Control-stack-delta emission rule (v0.4 addition — see §12):** a `Transition`'s control-stack deltas (pushes/pops) describe **surviving fibres only.** A fibre deleted in the same transition emits no pop deltas for its own stack — `fibers_delete` plus the relevant concurrency-table `Retire`/`Remove` mutations are the complete statement of what happened to it; a stack-pop delta for a fibre that no longer exists is redundant with its deletion and creates a K-2 verification oddity (checking stack↔membership consistency for a fibre that is, by the same transition, no longer live). K-2 (§7) is already scoped to live fibres ("for live F"); this rule makes explicit that delta *emission*, not just invariant-checking, respects that scope.

**Determinism rider (v0.4 revision — see §12 for the amendment history):** cancellation order is **record-nesting order, not fibre-depth.** The innermost concurrency-table *record* retires first (a child record — e.g. a race armed inside a forked branch — before its enclosing parent record, walking the record tree, not any fibre's control-stack depth); within one record's retirement, its member fibres are cancelled in fibre-ID order (context-derived IDs, stable under replay), never map iteration order. This is the total, replay-stable order: the record tree is a strict partial order over concurrency-table records, and the fibre-ID tiebreak totalizes it within a record — fibre-depth is not a safe substitute, because two fibres in structurally unrelated branches can sit at equal control-stack depth while holding differently-nested records, which depth-ordering cannot distinguish and record-tree-order can. Cancelling a fibre unwinds its own control stack innermost-first (same record-nesting order, applied to that one fibre's stack) before removal.

### Static structure vs. dynamic activation (v0.3 addition)

The compiled artifact is the whole taxonomy — every block that *could* execute; a token is one cursor in it. These never co-mingle:

> **Artifact addresses never appear in runtime state; runtime handles never appear in artifacts.**

The control stack does not hold program structure — it holds *activations* of static blocks: fresh concurrency-table records allocated per execution (a `GUARD>` in a loop arms a fresh record each iteration; each `FORK` execution allocates a fresh barrier activation). The relationship is exactly return stack to colon definition: activation records, not definitions. Static nesting is proven over the artifact (V-1/V-2); dynamic extent is instantiated per token and governed by K-1..K-3. Enforced by construction: `Addr` (artifact) and `RecordId` (runtime handle) are distinct types with no conversion; V-9 keeps runtime-consulted semantics out of the envelope; the K-invariants keep category (ii) below — runtime execution identity — out of records.

**Three categories of `Addr` presence in runtime state (v0.8 addition — see §16):** the rule above governs *use*, not mere presence. `ConcurrencyRecord::handler: Option<Addr>` has carried an artifact address since V1.2 (§5, `GUARD>`'s only sound encoding of its handler target) without ever having violated K-1..K-3 — the prohibition was never "no `Addr` in a record," even though earlier drafts of this section read that way. Three categories, only the second forbidden:

(i) **Code reference / jump target** (`handler`). Read only to resolve *where* the handler fibre spawns, never *which activation* it is. No alternative encoding exists — the target is inherently static, and resolving it is execution-affecting by design.

(ii) **Runtime execution identity — FORBIDDEN.** This is what the rule actually guards against. The exemplar is the historical failure this document exists to rule out: resolving a `FORK`/`JOIN` pairing by its static site instead of the inherited `RecordId` handle would make two concurrent activations of one static site indistinguishable, breaking re-entrancy (see V-3, §7 — static pairing annotations are proof material only; runtime resolution is exclusively via the handle for exactly this reason).

(iii) **Static-site identity for accounting that must span activations** (`opened_at: Option<Addr>`, §15 v0.7 ruling F) — a category the original text never contemplated. The property that disqualifies `Addr` for (ii) — a static site does not distinguish activations — is exactly the property that qualifies it here: a repeated-failure budget is deliberately *about* a static guard site's history across re-runs, not about any one activation. `opened_at` is read only to key an out-of-frame store aggregate (`guard_failure_budget`); `apply` never consults it to resolve control flow.

`handler` is category (i); `opened_at` is category (iii); nothing in this document has ever populated category (ii) — the K-invariants (K-1..K-3, §7) are what continue to enforce that, unchanged by this clarification. The lexical failure mode category (ii) forbids is the historical one — a token that *is* a pointer into the model (PlanWalker's `current_node_id`) or structure consulted as state (v1 side tables).

## 5. Decision D2 — Word inventory and perimeter

Stack-effect notation: `( operand-stack-before -- after )` `[ control-stack-before -- after ]`. All words are kernel-pure; effect-emitting words append to `Transition.effects`.

| Word | Effect | Semantics |
|---|---|---|
| `GUARD>` | `( handler-addr -- ) [ -- h ]` | Allocate interrupting-guard record, push handle. Handler addr is a verified code address. |
| `<GUARD` | `( -- ) [ h -- ]` | Pop and retire the guard. Verifier: must match its `GUARD>` on all paths (V-1). |
| `GUARD-N>` / `<GUARD-N` | as above | Non-interrupting guard: on trigger, spawn handler fibre *without* unwinding members; record **re-arms** after trigger (Q2 ratified, §13 amendment v0.5). Distinct opcodes, not a flag — the distinction must be visible to static analysis at the opcode level, exactly as `CALL`/`RET` are distinct instructions (review-ratified). **The one deliberate v1-coverage extension.** |
| `GUARD-R>` / `<GUARD-R` | `( -- ) [ -- h ]` | **New, §18 v0.10 (A18/ruling G).** Interrupting, data-rollback-capable, no handler: on trigger, unwind members (as `GUARD>`) and restore the A3 rollback set — `domain_payload`, business `flags`, `join_expected`, session stack — to its value at scope-open; loop/retry counters are never restored. Carries what §13 ruling B originally put on *every* guard; `GUARD>`/`GUARD-N>` no longer capture or carry a rollback snapshot. A third opcode, not a flag on `GUARD>` — same "distinct opcodes, not a flag" precedent as `GUARD-N>` above, and for the same reason: the verifier must see the disposition without inspecting operands (V-10 below). Rollback implies interrupting, so the combination space is three opcodes, not four (a non-interrupting, rollback-capable guard is incoherent — restoring the payload under members that keep running against the restored state has no sound meaning). `CANCEL-SCOPE` and automatic rollback-on-definitive-failure (§13 ruling C, §14 ruling D) operate exclusively on `GUARD-R>`-opened records now; targeting a plain `GUARD>`/`GUARD-N>` handle is a V-10 verify-time rejection. |
| `RACE{` | `( n -- ) [ -- h ]` | Open first-wins race record over the next n arms. |
| `ARM-TIMER` | `( duration -- ) [ h ]` | Emit `ScheduleTimer` effect bound to the race; deterministic timer ID per T5. |
| `ARM-MSG` | `( correlation -- ) [ h ]` | Register message alternative. |
| `ARM-EFFECT` | `( effect-desc -- ) [ h ]` | Arm an external-effect alternative. |
| `}RACE` | `( -- ) [ h -- ]` | Park on the race. A command addressed to an armed alternative resolves it: winner continuation runs; other arms' pending effects consumed/cancelled; losing members cancelled in fibre-ID order — one transition. |
| `FORK n` | `( addr1..addrn n -- ) [ s ]` | Allocate a fresh barrier *activation* record; create n fibres at given addresses, each inheriting the parent's control stack **with the barrier handle pushed on top**, and registered as members. Re-entry (a bounded loop containing this FORK) allocates a fresh activation per execution. Fibre IDs context-derived. **Correction (§18 Part 2 item (a)/(c) implementation, landed 2026-07-23 — the text below through v0.10 is factually wrong about the mechanism and is superseded in place, not merely fulfilled by coincidence):** this row previously claimed "the operand-stack `n` is the actual fan-out for this activation, computed at runtime" and that "`V2Fork`'s existing operand-stack encoding (targets pushed, `n` on top) needed no shape change." Checked against the real code rather than re-derived from this table: `Instr::V2Fork` (`bpmn-lite-types/src/types.rs`) is `{ targets: Box<[Addr]>, pairing: Addr }` — a compile-time-fixed array of jump addresses. There is no operand stack involved in `V2Fork` at all, dynamic or otherwise, and there never has been; the kernel handler (`bpmn-lite-kernel/src/lib.rs`) sets `arity = count = targets.len()` directly from the compiled array, unconditionally. **"Dynamic arity" is a compiler *lowering pattern*, not a kernel/type-level mechanism:** the compiler always lowers a gateway (or MI activity) to a `V2Fork` whose `targets` array has exactly the artifact-declared static maximum `n` (out-degree, or MI's declared cap — ruling K) — i.e. `V2Fork` always spawns `n` fibres, matching `targets.len()` exactly, as it already did before this ruling. A branch whose condition evaluates false (or whose MI index exceeds the actual runtime collection length) is compiled to jump straight to its own `V2Join` instead of doing real work, using only pre-existing opcodes (`BrIf`/`Jump`/`V2Join`) — that branch still executes a real `V2Join` arrival, so the barrier retires via the completely unmodified `V2Join` mechanism. Proven by hand-assembled fixture, not asserted: `bpmn-lite-kernel/src/lib.rs`'s `v2fork_mixed_real_work_and_skip_to_join_branches_retires_barrier_via_unmodified_mechanism` (a `V2Fork` with 3 static targets, one of which `BrIf`s straight to the shared `V2Join`; barrier retires correctly on the third arrival, `RecordCounters { arity: 3, count: 3 }` at birth, all 3 members deleted at retirement) and `bpmn-lite-types/src/artifact.rs`'s `max_fibers_already_accounts_for_a_v2fork_with_a_skip_to_join_branch` (`VerifiedLimits.max_fibers` is exactly 4 — 1 base + `targets.len()` — unaffected by which branch skips). See §7's V-3 entry for the corresponding correction. |
| `JOIN` | `( -- ) [ h -- ]` | Pop the inherited barrier handle; decrement *that activation*; park unless last arrival; last arrival continues and retires it. Resolution is by dynamic handle only — never by static identity. The static FORK/JOIN pairing id exists solely as a verifier annotation for V-3's arity proof. **Survivor semantics (v0.4 addition, see §12):** of the N fibres a `FORK n` created, the fibre whose `JOIN` is the *last arrival* is the sole survivor — it continues execution past the join point with the single post-join continuation. The N−1 non-last arrivals are **deleted at the moment the barrier retires** (not before — each parks at its own `JOIN` until then). This resolves what the barrier's fate alone (§4) left open: which physical fibre ID the join point's continuation belongs to. |
| `WAIT-FOR` / `WAIT-UNTIL` | `( duration\|deadline -- )` | Park + emit timer effect (T5 mechanism as a word). |
| `WAIT-MSG` | `( correlation -- )` | Park on message. |
| `AWAIT-EFFECT` | `( effect-desc -- )` | Emit `DurableEffect::Invoke` + park on completion; effect-ID derivation is the word's, per E5. |
| `CANCEL-SCOPE` | `( -- ) [ h ]` | Explicit cancellation reusing the compensation op (Q4 ratified, §13 amendment v0.5): pop the calling fibre's own handle, restore the scope's rollback snapshot, unwind nested members innermost-first (record-nesting order per ruling A), run no handler — the cancelling fibre continues in place. Not the earlier BPMN-terminate-only reading (no rollback); cancel-*events* still encode as guards. **Snapshot source corrected, §18 v0.10 (ruling G):** captured at `GUARD-R>` open only, not `GUARD>`/`GUARD-N>` — the latter two carry no snapshot as of ruling G, so `CANCEL-SCOPE` is only legal against a `GUARD-R>`-opened handle (V-10, §7, enforces this statically; targeting a plain `GUARD>`/`GUARD-N>` handle is a verify-time rejection). |

### Deferred to v3 (explicit non-coverage)

- **Compensation.** Reverse-order handler execution over completed activities with its own token semantics. Binding admission requirements on v2: concurrency-table records carry a `kind` discriminant with room for `Compensation`; retirement may *archive* rather than delete (compensation needs completed-scope history); the control stack accepts non-scope record kinds. The table and stack were renamed (from "scope table"/"scope stack") precisely because barriers already break the scope category and compensation will further — the structures are named for their role, not their first tenant.
- **Event subprocesses.** Expressible later as instance-root non-interrupting guards.
- **Multi-instance activities and inclusive (OR) gateways — NO LONGER DEFERRED (§18 v0.10, rulings H/K, reversing v0.9/§17's firm deferral).** The v0.9 reasoning (parallel MI needs dynamic FORK arity, which breaks V-3's static-arity theorem; a combination-enumeration lowering for inclusive gateways would preserve V-3 but the join side still needs dynamic arity, so both defer to the same v3 `FORK-DYN` design) is superseded, not wrong at the time — it assumed enumeration was the only way to keep V-3 static, and combination-enumeration genuinely is unworkable in the general case. Design B, ratified as §18 ruling H, finds a cheaper path: derive arity as the graph is traversed rather than enumerate it, and weaken V-3 from an equality proof to a static *bound* (`arity ≤ n`, `n` a gateway's out-degree — still statically checkable, still proof, not a runtime hope). `bpmn-lite-engine/src/tests.rs`'s `T-IG-1` through `T-IG-6` plus `T-AUTH-2` — the v1 inclusive-gateway specification preserved through V5 exactly for this moment — are the fixture corpus this lowering work starts from. See §18 rulings H and K for the full design and parallel MI's own additional deltas.
- **Compensation** and **event subprocesses** remain deferred to v3 as originally stated above — unaffected by this reversal, which concerns dynamic arity specifically, not the full v1-coverage gap.

## 6. Decision D3 — Persistence integrity model (corruption detection for the switch/persist/resume cycle)

### Reframing

There are no production instances; migration machinery is deleted from scope — cutover is wipe-and-recompile. Version fields survive with inverted purpose: each versioned surface accepts **exactly one value**; any other value is corruption or foreign data and refuses atomically. Versioning is a tripwire, not a dispatch key.

Objective: **cast-iron detection of corruption** across the context-switch cycle. Every park seals a frame; every resume proves the seal before a single instruction executes. Detection, not correction: fail-stop by design — never repair, never guess.

### Five concentric rings (revised structure per review R1)

Verification proceeds outermost-in on resume; each ring assumes nothing proven by an inner one.

**Ring 1 — Physical integrity.** Canonical bytes are the stored bytes. One deterministic encoding (BTreeMap-only, fixed field order); the frame is canonicalized, hashed, and *those bytes* persisted (BYTEA). On load: verify the hash over raw bytes **before decoding** — a corrupted frame never reaches the deserializer. Version tripwires are checked here, on the envelope, pre-decode. Operational introspection is a rebuildable JSONB projection, never authoritative (M2 doctrine).

**Ring 2 — Frame integrity.** The hash domain is the transition closure: `state_hash = BLAKE3(canonical(snapshot ‖ fibres by fibre-ID ‖ concurrency table ‖ pending-effect set ‖ revision ‖ artifact_hash))` — everything required to execute the next transition, resolving M9 by construction. Artifact binding lives inside the digest, so wrong-artifact execution is unrepresentable. The journal chains frames: records carry `prior_state_hash → new_state_hash`; the snapshot row stores its hash and producing sequence; resume verifies three-way agreement (recomputed == stored == `journal[last_seq].new_state_hash`). E3 makes torn commits impossible by design; the chain makes an E3 *implementation* failure detectable — the defended threat is future bugs, not only storage faults.

**Ring 3 — Runtime (semantic) integrity.** Unconditional structural asserts over the decoded frame at every park/resume: PC within program; operand/control stack heights within `VerifiedLimits`; every handle resolves in the concurrency table; membership↔control-stack consistency (shadow of K-2); every member references a live fibre (shadow of K-1); barrier counts ≤ static arity; every pending effect owned by exactly one waiting fibre. These are the runtime shadows of proven properties (V-1..V-9 static over the artifact; K-1..K-3 inductive over `apply`): a verified program under a proven kernel yielding a structurally invalid frame has one explanation — corruption — so the asserts are unconditional, fail-closed, O(fibres + records).

**Ring 4 — Replay integrity.** With zero version adapters in the system, T10 replay divergence has a single meaning: corruption. Nightly from-checkpoint replay fleet-wide; forensic from-genesis replay on every quarantine.

**Ring 5 — Quarantine.** Any ring fires ⇒ typed `IntegrityError` variant naming the ring, atomic quarantine, readiness reflects it, zero partial reads of the condemned frame.

### Failure-model coverage

| Threat | Ring |
|---|---|
| Torn / partial write | E3 (prevention) + Ring 2 chain (detection of E3 failure) |
| Bit rot, storage fault, truncation | Ring 1 + Ring 2 |
| Kernel bug writing inconsistent state | Ring 3 + Ring 4 |
| Stale concurrent writer | Fence (E2, landed) |
| Wrong-artifact execution | Artifact hash inside Ring 2 |
| Foreign / stale-format / hand-crafted data | Ring 1 tripwires |

### Named risk R1 — canonical-form drift

Every ring above Ring 1 rests on it: nondeterministic serialization (map ordering, float formatting, enum representation, serde attribute drift, dependency upgrades altering encodings) diverges hashes without corruption — or hides a defect inside tolerated nondeterminism. Mitigations: (a) the canonical encoding is a hand-audited, dependency-pinned module with a golden-bytes corpus (fixture frames whose exact bytes are committed and diffed in CI); (b) round-trip fixed-point law `canonicalize(decode(bytes)) == bytes` for every fixture and property-fuzzed frame; (c) sampled runtime round-trip assertion on commit; (d) any encoding change is definitionally a tripwire change and a wipe — there is no "compatible" encoding change.

## 7. Decision D4 — Proof obligations

Two kinds of theorem, deliberately separated (review R1: member-liveness cannot be an artifact theorem — no static analysis of a program proves a property of frames; what is provable is preservation by the kernel).

### Artifact theorems V-1..V-9 — static, per-CFG-edge dataflow, discharged by the T6.3 verifier

- **V-1 (control-stack balance).** On every fibre-reachable path, the abstract control stack at `<GUARD`/`}RACE` matches the corresponding opener; at every fibre `END`, the control stack is empty.
- **V-2 (proper nesting).** Openers/closers well-bracketed on all paths; no path closes a record opened on a different path without a join point at equal control depth.
- **V-3 (arity agreement).** **Correction (§18 Part 2 item (a)/(c) implementation, landed 2026-07-23):** this entry previously said V-3 "weakens from equality to a static bound" (`arity ≤ n`) because `FORK`'s `n` was believed to become a runtime-computed operand-stack value. That premise is false — see §5's `FORK n` row for the corrected mechanism. Because `V2Fork`'s `targets` array is (and remains) the static compile-time bound `n` by construction — the compiler always emits exactly `n` targets, with branches that don't apply lowered to skip straight to `V2Join` rather than the target list shrinking or becoming dynamic — `targets.len()` already **is** the static bound V-3 needs, not a separate runtime quantity to be checked against one. **V-3's own text and code need no change at all:** it remains exactly what it always was, the static referential pairing check (`v2_verifier.rs`'s `Instr::V2Join` arm — "the pairing must reference an actual `V2Fork`") plus the kernel's unconditional `arity = count = targets.len()` at `FORK` time (K-3, §7 below). What changed is only the *understanding* of what compiles to a `V2Fork`: a gateway/MI construct with fewer live branches than its static maximum still lowers to a `V2Fork` with the full static-maximum target count, some of whose targets do no real work. `VerifiedLimits.max_fibers` (`targets.len()`-based, `bpmn-lite-types/src/artifact.rs`) needed no fix for the same reason — proven by `max_fibers_already_accounts_for_a_v2fork_with_a_skip_to_join_branch` (`bpmn-lite-types/src/artifact.rs`). Static pairing annotations are unique per artifact and referenced by exactly one FORK/JOIN pair, unchanged. The annotation is proof material only — runtime resolution is exclusively via the inherited handle, so re-entrant FORKs (each execution a fresh activation) are sound by construction, unchanged. **Further correction (§19 v0.11, landed 2026-07-24):** "the pairing must reference an actual `V2Fork`" is what V-3 checks, but the *compiler's own derivation* of which `V2Fork` a given `V2Join`'s pairing annotation names was found to be capable of silently naming the WRONG (still arity-valid) one — see §19 for the full defect, Adam's ruling, and the fix. V-3's text above is unaffected and required no change; only the mechanism supplying its input did.
- **V-4 (handler validity).** Every guard handler address is a valid instruction with well-typed entry state (operand + control stacks) given the guard's extent; handler CFGs terminate in `END` or rejoin at legal state.
- **V-5 (race shape).** `RACE{ n` followed by exactly n `ARM-*` words before `}RACE` on all paths; arms are effect/message/timer registrations only — no unguarded state mutation between arm and park.
- **V-6 (operand-stack safety).** Unchanged from T6.3, extended over all v2 words' operand effects.
- **V-7 (limits).** `VerifiedLimits` gains `max_control_depth`, `max_barriers`, `max_records`; computed maxima embedded in the envelope; Ring 3 asserts them.
- **V-8 (bounded flow).** Backward-flow bounds hold across handler and race-resolution edges; guards create no unbounded re-entry.
- **V-9 (dictionary purity).** The v2 envelope structurally contains no execution-time-mutable semantics: symbols, debug, FFI declarations, correlation-key schemas, DSL vocabulary pin only. The race-plan/join/boundary-route tables do not exist in the v2 envelope type.
- **V-10 (rollback dominance). New, §18 v0.10 (A18/ruling G), landed and tested (`bpmn-lite-types/src/v2_verifier.rs`) ahead of this write-up.** A `GUARD-R>` scope must dominate any `FORK` within its extent — it may contain a complete `FORK`/`JOIN` region, but must not be contained by one. Statically checkable on the CFG alongside V-1/V-2's bracketing analysis: `GUARD-R>`'s open is rejected if a `Barrier`-kind token is already on the entry control stack. Consequence, not a separate proof: a rollback-capable scope is instance-wide in its data effect (A3, ruling G below), so this theorem forces it to be instance-wide in its control extent too, which makes barrier starvation via the rollback path unconstructible rather than merely detected at runtime (see Ring 3's companion assert, D3 §6, which stays as the standing proof this theorem continues to hold in production, not as the mechanism that makes the class safe).
- **V-11 (terminal reachability). New, §18 v0.10 (ruling J).** Every instruction has a path to a terminal instruction (`END`/`END-TERMINATE`) — the dual of the existing start-reachability walk. **V-8 interaction, flagged explicitly:** backward edges are rejected unconditionally by V-8's bounded-flow analysis, so a loop whose only exit is conditional still needs a forward path to terminal; V-11's walk treats loop-exit edges as ordinary forward paths, not as a special case requiring separate justification — the loop body's back-edge is invisible to V-11 (V-8 already forbade it as a *forward* path), and the exit edge is exactly the path V-11 requires to exist. A compiler that emits an unreachable-to-terminal node (a gateway branch with no route to any `END`) fails V-11 at compile time; this is distinct from, and does not replace, the runtime zero-match rule (ruling J) — V-11 proves every *declared* path reaches a terminal, zero-match is what happens when a *dynamic* decision (a gateway's actual condition evaluation) matches none of the declared routes.

### Kernel preservation theorems K-1..K-3 — inductive invariants of `apply`, discharged per-word, property-tested, shadow-asserted by Ring 3

For every command and every word, `apply` preserves:

- **K-1 (member liveness).** Every member of every live concurrency record references a live fibre. (Every fibre-terminating word — `END`, cancellation, race-loss — deregisters the fibre from all records; every registering word registers only live fibres.)
- **K-2 (stack↔membership consistency).** A fibre's control stack and the records' membership sets agree: `h` on fibre F's stack ⟺ F ∈ members(h), for live F. (D1's consistency law.)
- **K-3 (barrier soundness).** For every live barrier: `0 ≤ count ≤ arity`, and `count` equals arity minus the number of member fibres that have executed its `JOIN`; retirement occurs exactly at zero.

Discharge protocol: each word's implementation carries its K-preservation argument as a doc-comment proof sketch; property tests generate word sequences and check K-1..K-3 after every `apply`; golden-transition tests pin exact frames. Ring 3 asserts the same facts in production — a proven invariant with a shadow, not a runtime hope.

Property-test obligation: fuzzed programs violating any V-theorem are rejected with typed errors, none panic (extends T6.6); fuzzed command sequences never produce a K-violation (a K-violation in test is a kernel defect, not an input defect).

## 8. ABI and schema impact summary

| Surface | Change | Version mechanism |
|---|---|---|
| Instruction set | **+24 words (corrected count, post-close remediation — `GUARD-TIMER-CYCLE>` restoring V&S §13 amendment v0.5 ruling A to `GUARD-TIMER>`'s own timer-fire path, landed 2026-07-23, see below).** v0.9's +16 plus `GUARD-R>`/`<GUARD-R` (A18/ruling G, landed) plus `GUARD-TIMER>` (ruling I Part 2, landed) plus `Instr::V2RouteZeroMatch`/`Instr::V2LoadPlaceholderMatch` (item (e), landed) plus two more, added while building ruling K's multi-instance frontend lowering (the final §18 implementation-sequence step): `Instr::V2MiIndexLive` and `Instr::V2MiArityCheck`. **Neither is ratified D2 vocabulary**, same class as `V2RouteZeroMatch`/`V2LoadPlaceholderMatch` immediately above — V5-lowering-internal glue, documented at the same rigor in `Instr`'s own doc comment (`bpmn-lite-types/src/types.rs`) and `EOP-PLAN-BPMN-ISA-002.md`'s ruling K writeup. `V2MiIndexLive` (`( -- bool )`): the MI-side analogue of `LoadFlag`/`V2LoadPlaceholderMatch` — pushes whether a synthesized branch's static index is below the runtime collection length, the per-branch skip check `V2Fork`'s dynamic-arity pattern needs for MI exactly as `LoadFlag` supplies it for a gateway condition. `V2MiArityCheck` (`( -- )`, unconditional, runs once immediately before `V2Fork`): if the runtime collection length exceeds the artifact-declared max, returns `TransitionError::ResourceLimitExceeded` — the SAME shape `validate_snapshot_limits` already uses for fiber/stack/register overflow, a hard non-resumable reject, deliberately NOT `V2RouteZeroMatch`'s `Incident`-raising mechanism (ruling K item (c): MI's empty-collection legality and a gateway's zero-match incident are not unified; over-capacity is a third, distinct case from both — a genuine artifact/input-data invariant violation, not a routing gap). **Scope note, load-bearing for both new words:** this codebase has no array/collection-valued `Value`/`DataObjectType`/`BindingSource` at any layer (checked, not assumed) — MI's `inputCollection` is represented here by its RUNTIME LENGTH only (an `I64` flag), not its elements; per-element value access into the inner activity's own task body is out of scope for this landing (see `Instr::V2MiIndexLive`'s doc comment and the plan-doc writeup's fork note). `GUARD-TIMER>`: Q6's guard-arming-trigger is a NEW, distinct opcode — not an operand-encoding extension of `GUARD>`/`GUARD-N>`/`GUARD-R>` as this row previously speculated — mirroring how `V2ArmTimer` arms an already-open `RACE{` record via `fiber.control_stack.last()`, so the three guard-open words keep their existing zero-operand encoding completely unchanged (a guard opened without this word is byte-for-byte what it was before). `( duration -- )`, verifier-enforced to immediately follow the guard-open instruction it arms; on fire, issues the same effect as `Command::V2TriggerGuard` for `V2Guard`/`V2GuardN`, or automatic rollback for `V2GuardR` (see `bpmn-lite-kernel`'s `apply_timer`/`apply_v2_guard_timer_rollback`). Race/join/boundary side tables removed from envelope, unchanged from v0.3. **Correction (V5.3 implementation, landed 2026-07-23):** the sentence above described a v2-envelope-construction-time fact (a v2 program never populated those tables); it did not mean the Rust types had been deleted, and until this landing they hadn't been — `race_plan`/`boundary_map`/`RaceId`/`RacePlanEntry`/`WaitArm`/`WaitAny` and v1's `Fork`/`Join`/`WaitFor`/`WaitUntil`/`WaitMsg`/`ForkInclusive`/`JoinDynamic`/`ForkPayload` all still existed as constructible types, with live kernel `apply` handlers, until V5.3 deleted them outright (`bpmn-lite-types/src/types.rs`, `bpmn-lite-kernel/src/lib.rs`). This row's own word-count arithmetic was never about counting the ABI table's *removals* (only its D2-word *additions*) — per `EOP-PLAN-BPMN-ISA-002.md`'s own "kernel LOC delta is an exit metric, net negative expected" framing (§8/ABI-impact table), V5.3's net kernel-side change is a substantial deletion (multiple `Instr`/`WaitState`/`TimerKind` variants, their `apply`/`verify_bytecode`/canonical-encoding arms, and the `race_plan`/`boundary_map` mechanism removed from `CompiledProgram`/`ArtifactEnvelope` entirely) — see the plan doc's V5.3 entry for the full receipt. This table's "+23 words" figure is unaffected by the correction (it was never counting v1 removals to begin with); recorded here so a reader doesn't mistake the pre-existing sentence for a claim already discharged. **Second correction, same day, post-close (not part of any prior tranche's sign-off — see `EOP-PLAN-BPMN-ISA-002.md`'s post-close entry, "EndTerminate V-1 exemption + GUARD-N> timer re-arm restoration"):** `Instr::V2GuardTimerCycle` (word `GUARD-TIMER-CYCLE>`, `( -- )`) is the +24th word — a post-close restoration of §13 amendment v0.5 ruling A ("`GUARD-N>` re-arms after trigger... this is the default") to `GUARD-TIMER>`'s own timer-fire path specifically, not a new design. Optional and additive: must immediately follow `GUARD-TIMER>` (verifier-enforced by instruction shape, `v2_verifier.rs`) and only ever bounds a `GUARD-N>` target — `GUARD>`/`GUARD-R>` retire their record on trigger regardless, unaffected. Without it, a `GUARD-TIMER>`-armed `GUARD-N>` now re-arms UNBOUNDED by default (matching ruling A's own "nothing... expresses a non-re-arming variant" framing) rather than ruling I's original fire-once behaviour; `GUARD-TIMER-CYCLE> { max_fires }` narrows that default to a finite count, mirroring BPMN `timeCycle`'s bounded-repeat syntax (`R<n>/PT<duration>`). Mechanism: `bpmn-lite-types::transition::TimerRepeatSpec`/`TimerMutation::Rearm` — pre-existing generic timer-schedule infrastructure (present since before this landing, unused until now) — not a new type. Frontend lowering remains out of scope, matching `GUARD-TIMER>` itself: `lowering.rs`'s `timer_spec_duration_ms` still silently drops `TimerSpec::Cycle`'s `max_fires` to a single relative duration; BPMN XML `timeCycle` does not reach `GUARD-TIMER-CYCLE>` yet. V-8 (backward-edge/bounded-flow theorem): confirmed NOT implicated — re-arming is a runtime effect-schedule decision inside `apply_timer`'s own handler, invisible to the static CFG `successors()`/`verify_v2_control_stack` walks V-8 depends on; `GUARD-TIMER-CYCLE>` introduces no new instruction-graph edge at all (falls through to the generic `address + 1` case, same as `GUARD-TIMER>` itself). Full detail, receipts, and the K-invariant/`max_fibers` static-accounting corollary this restoration also required: `EOP-PLAN-BPMN-ISA-002.md`'s post-close entry. **Third correction, same day, post-close — ruling K Part 2 (per-element value access, landed 2026-07-23):** the "no array/collection-valued `Value`" scope note directly above is superseded — `Value::Array` now exists (see ruling K's own Part-2 correction, §18 above). `Instr::V2MiIndexLive`/`V2MiArityCheck`'s `length_flag` field is renamed `collection_flag` and reads a `Value::Array` directly (length derived via `.len()`, not tracked as a redundant separate `I64`). A new word, `Instr::V2MiLoadElement { collection_flag, index }` (`( -- value )`), is the +25th instruction — same non-ratified-D2-vocabulary class as its two siblings — running on the live path immediately after `V2MiIndexLive`'s `BrIfNot`, delivering that branch's own element by clone (Adam's by-value ruling) into a synthesized per-branch flag via the pre-existing `StoreFlag`, which then reaches the inner activity's job through the SAME `orch_flags` pipeline every other service task's inputs use. `V2Fork` needed no shape change (reconfirmed). Full record: `EOP-PLAN-BPMN-ISA-002.md`'s ruling K Part 2 entry. **Fourth correction, same day, post-close — `GUARD-TIMER-CYCLE>` frontend wiring closed (landed 2026-07-23):** the Second correction's own "Frontend lowering remains out of scope... BPMN XML `timeCycle` does not reach `GUARD-TIMER-CYCLE>` yet" is superseded — it now does. No new opcode: `lower_boundary_guarded_task_v2` (`ServiceTask`) and the `IRNode::FfiServiceTask` boundary-timer arm (`bpmn-lite-compiler/src/lowering.rs`) now emit `Instr::V2GuardTimerCycle { max_fires }` immediately after `V2GuardArmTimer` whenever the boundary timer's `TimerSpec` is `Cycle` (non-interrupting only — see below); `timer_spec_duration_ms` is unchanged, still extracting only `interval_ms` for the `PushI64` operand, exactly as designed (`max_fires` is a separate static field, not folded into that helper). An interrupting boundary timer with a `Cycle` spec was already rejected pre-existing this fix, at `verify_or_err` time (`bpmn-lite-compiler/src/verifier.rs` §7b, "cycle timers must be non-interrupting") — this fix's own lowering-site `debug_assert!(!interrupting, ...)` documents that invariant rather than re-deciding it. One adjacent, pre-existing, unrelated gap found and fixed while wiring this: `verify_or_err`'s §7a `attachedToRef` host-existence check for `BoundaryTimer` omitted `IRNode::FfiServiceTask` from its match (only `ServiceTask`/`HumanWait` were accepted), even though `lowering.rs` already had full `FfiServiceTask` boundary-timer lowering support — meaning no BPMN XML could ever reach that lowering code end-to-end, for ANY boundary-timer spec, not just `Cycle`. Fixed (added the missing match arm); not a design fork, `FfiServiceTask` is exactly as valid a boundary-timer host as `ServiceTask` and nothing in the ratified word set treats it differently. Unbounded `timeCycle` syntax (`R/PT<duration>`, no repeat count) remains genuinely unsupported — confirmed by direct test (`parser::parse_iso_cycle("R/PT1H")` is `Err`, not a `u32::MAX`/sentinel admit) — and is explicitly NOT decided by this fix: `TimerSpec::Cycle.max_fires` is a plain `u32` with no distinct "unbounded" representation, so admitting it would require a real new design decision (a new variant or reserved sentinel, plus verifier support), out of scope for wiring an already-ratified, already-bounded word through the frontend. Full detail, receipts: `EOP-PLAN-BPMN-ISA-002.md`'s post-close entry. | `ArtifactAbi` single-value tripwire; no translator |
| Fibre snapshot | + control stack | `SnapshotSchema` single-value tripwire; no upgrade fns |
| Snapshot | + concurrency table (records: id, kind, members, handler, state, counters); persisted as canonical BYTEA per Ring 1. **§18 v0.10 (ruling G):** three new optional fields for `GUARD-R>`-only rollback capture — `rollback_flags`, `rollback_join_expected`, `rollback_session_stack` — alongside the existing `rollback_domain_payload`/`hash` pair (§13 v0.5), all `None` for `GUARD>`/`GUARD-N>` records. Barrier activation records now carry per-activation actual arity (ruling H) in addition to static arity. | same tripwire |
| Process state | **New, §18 v0.10.** `ProcessState::Incidented{incident_id}`, split from `Failed` — `Failed` was doing two jobs (permanently dead; resumable via `ResolveIncident`) under one name. Two exhaustive predicates, `is_terminal()` (unchanged meaning, false for `Incidented`) and new `is_schedulable()` (true only for `Running`), each a compile error away from silently absorbing a future `ProcessState` variant — same §14 meta-rule (no wildcard arm) applied to a second classification. | same tripwire |
| Journal | + events: `ScopeArmed`, `ScopeRetired`, `RaceResolved{winner}`, `ScopeCancelled`, `HandlerSpawned`; + `prior_state_hash`/`new_state_hash` chain (Ring 2) | journal schema single-value tripwire |
| Kernel | race-resolution and boundary-promotion interpretation deleted; word execution + K-theorem discharge added | LOC delta is an exit metric (net negative expected) |
| Verifier | dual-stack interpretation, V-1..V-9 | — |
| Effects/timers/commit protocol | **no change** | — |

## 9. Inputs required before ratification

1. **Fixture corpus:** existing join/race/boundary/timer tests + EOP-EX-BPMN-ISA-002's adversarial workflow (interrupting guard over a parallel subprocess nested inside a race with a message alternative), hand-lowered with full dual-stack traces including the cancellation cascade. Drafted only after D1/D2 ratify, then locked as oracle. **Interpretation note (v0.4 addition, §12):** "a parallel subprocess nested inside a race" is realized as a `V2Fork` (the parallel subprocess) with one branch containing the race, not a race with a fork/join as one of its arms — a race's arms are effect/message/timer *registrations* (V-5: "no unguarded state mutation between arm and park"), and durable parallel work (a `FORK`, which allocates a barrier activation and creates member fibres) is not a registration a race arm can hold; the fork must be the outer structure. `EOP-EX-BPMN-ISA-002` is locked under this reading.
2. **Corruption-injection fixture set:** for each D3 ring, at least one deliberately damaged frame (flipped byte under the hash, chain break, dangling handle, membership asymmetry, over-arity barrier, orphaned pending effect, wrong tripwire version) proving the ring fires with the correct typed `IntegrityError` and quarantines atomically. Committed alongside the golden-bytes corpus.
3. **DSL lowering confirmation — SATISFIED (v0.9, §17, confirmed 2026-07-22).** Every DSL construct that emits an `Instr` today maps onto a D2 word with no residue. The word set in fact has *spare* capacity (`AWAIT-EFFECT`, `RACE{`/`ARM-TIMER` have no DSL emitter yet, since the DSL has no timeout/callout-await AST nodes) — spare capacity is not residue; this input specifically asks whether DSL output lacks a word, not the reverse. Gated V5's entry. **Still satisfied after §18 v0.10:** none of ruling H/K's dynamic-arity work adds a new DSL-side construct without a D2 target — `FORK`'s widened arity and the MI/inclusive-gateway lowering are new *frontend lowering* work (Part 2, item e of §18's implementation sequence), not new DSL AST surface this input governs.

4. **Fixture corpus extension for §18's new theorems — CLOSED (2026-07-23), not folded into the oracle, deliberately.** V-10 (dominance), V-11 (terminal reachability), and the dynamic-arity bound (V-3 as weakened) all now have complete fixture corpora (positive-admitted and negative-rejected) in `bpmn-lite-types/src/v2_verifier.rs`, landed by Part 2 items a/c and the A18 Part 2 tranche. Whether to also fold them into `EOP-EX-BPMN-ISA-002` (left open here as "if warranted, separate follow-on work") is now decided: **not warranted**, and recorded as such in that document's own §5 addendum, not left silently unaddressed — the oracle's purpose is a runtime `Transition`-shape trace for one adversarial program, while V-10/V-11/V-3 are static compile-time admission theorems with no `Transition` to trace; forcing them into that format would duplicate `v2_verifier.rs`'s own more-direct fixture pairs without proving anything the oracle's own scope calls for.

## 10. Questions for reviewers

- ~~Q1~~: **Discharged by evidence, not proof (v0.9, §17).** Closed via property-fuzz + shadow-assert coverage rather than an adversarial-review dialogue like Q2/Q4 — a different discharge mechanism, recorded as such, not silently treated as equivalent.
- ~~Q2~~: **Ratified (§13, amendment v0.5).** `GUARD-N>` re-arms after trigger by default.
- ~~Q3~~: **Fully closed, both halves (§18 v0.10 ratifies the second).** Whole-frame BLAKE3 (not per-fibre digests) is what `bpmn-lite-store-postgres` implements and has shipped since V2.2 — settled by what was actually built (v0.9, §17). The projection sub-question, left genuinely deferred at v0.9, is now ratified: **no JSONB projection ships in v2.** Operational introspection stays the rebuildable, non-authoritative Ring 1 projection already described (§6, M2 doctrine); a queryable structured projection remains buildable later without touching the hash domain, so deferring it costs nothing now and isn't a decision under time pressure — it's simply not needed yet.
- ~~Q4~~: **Ratified (§13, amendment v0.5).** `CANCEL-SCOPE` reuses the compensation op: rollback-snapshot restore + no-handler cancellation, not the plain terminate reading. Cancel-*events* still encode as guards.
- ~~Q5~~/Gap 1: **REOPENED AND RE-RULED (§18 v0.10, ruling H) — the v0.9 firm deferral is overturned, not merely revisited.** v0.9 (§17) closed this "not required in this V&S, firm deferral to v3" on the strength of a repository-evidence question (do any live runbooks need it) that the document itself said no evidence alone could settle — the deferral was a scope call under the assumption that supporting dynamic arity required either enumeration (inclusive gateways) or breaking V-3 outright (parallel MI), both genuinely expensive. That assumption is what changed: Design B (ruling H) derives arity from graph traversal instead of enumerating it, and weakens V-3 to a static bound instead of breaking it — cheaper than the v0.9 deferral decision accounted for, because the option wasn't on the table yet. Reopening a "firm" ruling is not undertaken lightly; it is not being reversed because the original ruling was wrong given what was known, but because what's knowable changed. Ratified: **dynamic-arity concurrency (parallel multi-instance, inclusive gateways) is IN for v2.** See §18 ruling H for the design and ruling K for parallel MI's own additional deltas. `bpmn-lite-engine`'s `T-IG-1..6`/`T-AUTH-2` remain the fixture corpus this lowering work starts from — the v0.9 asset survives the reversal of the v0.9 ruling that named it.
- ~~Q6~~: **Ratified (§18 v0.10, ruling I).** Guards carry an arming trigger, timer as the first kind — a boundary timer is a guard with a deadline, indifferent to whether the work inside is synchronous, async/effect-based, or a nested subprocess. **Retroactively resolves EX oracle §4 open item 1** ("exact `Command` shape for triggering an armed guard," deferred at V4 entry, never actually settled — `Command::V2TriggerGuard` exists but nothing in the word set showed what issues it): the arming trigger is what issues it. Full detail — including the rejected alternatives (a fourth `V2RaceArm::Internal` variant; treating service tasks as effects) and why — in §18 ruling I. Historical context (v1's mechanism, the `V2RaceArm`/`V2Guard` gap that opened this question) remains in `EOP-PLAN-BPMN-ISA-002.md`'s V5.1 boundary-timer entry, not repeated here.

## 11. Review R1 disposition

| Reviewer point | Disposition |
|---|---|
| "Derivable membership" is false once cancellation/death exist | **Accepted; strengthened.** Both structures canonical; consistency is invariant K-2 (§4, §7). |
| Distinct opcodes for non-interrupting guards, never a flag | **Already proposed; ratified.** Rationale recorded at the word (§5). |
| Barrier counts are synchronisation, not scope → rename table | **Accepted.** "Concurrency table"; glossary separates scope/guard/race/barrier/boundary (§2). |
| Scope stack is the return-stack analogue → "control stack" | **Accepted.** Renamed throughout; v3 record kinds (compensation, transactions) push onto the same structure by design (§2, §5). |
| Split D3 into physical/semantic; concentric structure | **Accepted.** Five rings, outermost-in verification (§6). |
| Missing theorem: no live scope references a retired fibre | **Accepted with refinement.** Not statically provable over artifacts; recast as kernel preservation theorem K-1, inductively discharged per-word, shadow-asserted by Ring 3 (§7). |
| Explain why FORTH; cite structured-concurrency lineage; box the principle | **Accepted.** §1 additions; boxed principle at head. |
| Terminology drift (scope/guard/boundary/race/barrier) → glossary | **Accepted.** §2, binding on generated identifiers. |

## 12. Amendment v0.4 (2026-07-21)

**Why this amendment exists:** D1–D4 ratified at v0.3 and unblocked drafting of `EOP-EX-BPMN-ISA-002` (the worked-example oracle) per §11's closing note. Drafting that oracle — specifically, hand-tracing the cancellation cascade for an interrupting guard over a forked region containing a nested armed race — surfaced three points v0.3 left underdetermined. Rather than let the oracle (or, worse, V4's kernel implementation) silently fill these gaps with an un-reviewed convention, they are ratified here as a conscious, versioned amendment. This is the freeze discipline working as intended: frozen means changes are deliberate and reviewed, not that the document is inviolable. Six months from now, "why does cancellation use record nesting?" has an answer that points here.

**A — Cancellation order is record-nesting order, not fibre-depth (§4).** The v0.3 determinism rider said cancellation/loser-unwinding is "fibre-ID order... innermost-first" without specifying what "innermost" ranges over. The oracle's first draft read it as fibre control-stack *depth* (deeper fibre cancels first) — this happened to give the correct answer for that specific scenario, but the primitive is wrong in general: two fibres in structurally unrelated branches can sit at equal control-stack depth while holding differently-nested concurrency-table records, and depth-ordering cannot distinguish them. The corrected primitive is the **record tree**: concurrency-table records form a strict partial order (a record opened inside another record's dynamic extent is its child); cancellation retires the innermost record first, walking that tree, and within one record's retirement its member fibres cancel in fibre-ID order. This is total and replay-stable — the record tree gives a strict partial order, fibre-ID totalizes ties within a record — which fibre-depth is not guaranteed to be. Determinism-critical: this governs the exact byte sequence of every cancellation-cascade transition, which is exactly what gets hashed and replayed.

**B — `JOIN` survivor semantics (§5).** v0.3's `JOIN` row pinned the barrier record's fate ("last arrival continues and retires it") but not which of the `FORK`'s N spawned fibres physically survives. Ratified: the last-arriving fibre is the sole survivor and continues past the join point; the N−1 non-last arrivals are deleted at the moment the barrier retires (each having parked at its own `JOIN` until then, not deleted earlier). This closes a real gap — without it, "last arrival continues" doesn't say which fibre ID owns the post-join continuation, information V4's `apply` needs to construct a well-formed `Transition`.

**C — No control-stack deltas for deleted fibres (§4).** `Transition.control_stack_deltas` (V1's declared surface, V4's sole producer) describes surviving fibres' stack changes only. A fibre deleted in the same transition (per B, or by cancellation per A) is fully described by `fibers_delete` plus the relevant concurrency-table `Retire`/`Remove` mutations; emitting a pop-delta for a fibre that no longer exists is redundant and creates a K-2 verification oddity, since K-2 is already scoped to live fibres. This is an emission-discipline clarification, not a new invariant.

**Scope of this amendment:** all three rulings are D1-level (concurrency-table/control-stack semantics), not new decisions (D5, D6, ...) — they close gaps within D1's existing shape, so they're recorded as amendments to §4/§5 rather than new top-level sections. Nothing here alters D2's word inventory, D3's persistence model, or D4's theorem list beyond what A/B/C's text says. `EOP-EX-BPMN-ISA-002` is updated to match this amendment and locked against v0.4, not v0.3.

---

## 13. Amendment v0.5 (2026-07-21)

**Why this amendment exists:** building V4.1's kernel words surfaced Q2 and Q4 (§10) as live blockers — `GUARD-N>`'s re-arm behaviour and `CANCEL-SCOPE`'s actual semantics had no ratified answer, and `CANCEL-SCOPE`'s ratified answer in turn exposed a third, larger question with no prior §5/§10 entry at all: what happens when an activity *inside* an interrupting guard fails outright. All three are recorded here together since B and C below are direct mechanical consequences of A.

**A — `GUARD-N>` re-arms after trigger (Q2).** On trigger, `GUARD-N>` spawns its handler fibre without unwinding members (as already specified) and the guard record stays `Armed` — it is not retired, so it can trigger again. This is the default; nothing in v2 currently expresses a non-re-arming variant, and none is added here.

**Extended, post-close, same-day (2026-07-23) — `GUARD-TIMER>`'s own timer-fire path restored to this same default.** Ruling I (landed after this amendment) built `GUARD-TIMER>` fire-once, missing this ruling's own default for the timer-triggered case specifically (the manual `Command::V2TriggerGuard` path already re-armed correctly; the timer path did not). Fixed at the kernel level, bounded by a new optional word, `GUARD-TIMER-CYCLE>` — see §8's Instruction-set row for the full mechanism and `EOP-PLAN-BPMN-ISA-002.md`'s post-close entry for receipts. Not a new ruling; this note exists so a reader of ruling A alone sees the gap was found and closed, not left standing.

**B — `CANCEL-SCOPE` reuses the compensation op, not plain BPMN-terminate (Q4).** The v0.3 working assumption (unwind members, no handler — pure terminate semantics) is superseded. Ratified: every `Guard`-kind scope (`GUARD>`/`GUARD-N>` alike) captures a **rollback snapshot** of the process instance's `domain_payload` at the moment it opens — "a standard lifecycle snapshot," not opt-in, not a separate word. `CANCEL-SCOPE` pops the calling fibre's own handle, restores that snapshot, unwinds nested members innermost-first (ruling A's record-nesting walk, §12), and runs no handler — the cancelling fibre continues in place past the instruction. This is deliberately narrower than `RecordKind::Compensation`'s full "reverse-order handler execution" (§5) — that record kind stays uninhabited by v2; this rollback is a property of `Guard`-kind records specifically, implemented as two new optional fields on `ConcurrencyRecord` (`rollback_domain_payload`, `rollback_domain_payload_hash`), not by instantiating `Compensation`. Cancel-*events* still encode as guards, unchanged from the v0.3 reading.

**Superseded in part by §18 v0.10 (A18/ruling G).** "Every `Guard`-kind scope... not opt-in" is the root error A18 corrects: it coupled control disposition (what happens to fibres when a scope fires) with data disposition (whether payload is restored), which BPMN's own boundary events do not couple — an interrupting boundary event redirects control, it does not restore data. As of §18, the rollback snapshot is captured only by `GUARD-R>` (a new, third opcode); `GUARD>`/`GUARD-N>` carry no snapshot at all. `CANCEL-SCOPE`'s mechanics described here (pop, restore, unwind innermost-first, no handler, calling fibre continues) are otherwise unchanged — only which records it may legally target changed (`GUARD-R>`-opened only, enforced by V-10, §7). The rollback set restored is also widened past `domain_payload` alone — see §18 ruling G, A3.

**C — Automatic rollback on definitive failure, interrupting guards only.** The most consequential piece: inside an **interrupting** `GUARD>` scope (not `GUARD-N>`), a *definitive* job/effect failure — one that has exhausted retry and matches no `error_route_map` entry, i.e. exactly the point v1's `apply_job_failure` would otherwise create an `Incident` — instead performs `CANCEL-SCOPE`'s exact restore-and-unwind operation automatically, with no `CANCEL-SCOPE` instruction required in the program. This replaces v1's error-class taxonomy and incident/routing machinery *for that one boundary, inside an interrupting guard scope only*: no distinction between `ContractViolation`/`BusinessRejection`/exhausted-`Transient`, no error routing, no incident. Explicitly out of scope for this rule, each independently reasoned:
  - **A retriable `Transient` failure is not "a fail" yet** — it can still succeed, so it is retried exactly as today (`JobRetryScheduled`); the rule only fires once a failure is definitive.
  - **A timer/wait resolving is not a failure at all** — it's ordinary forward progress (a race losing an arm, a wait elapsing), handled entirely outside `apply_job_failure`, and this rule has no reach there.
  - **`GUARD-N>` scopes are unaffected** — non-interrupting guards don't unwind on trigger by design (ruling A above), so "roll back on fail" doesn't fit their model; today's v1 incident/routing path is unchanged for fibres whose innermost armed guard is non-interrupting, or who sit inside no guard scope at all.
  - **The triggering fibre is killed, not continued or auto-respawned.** Unlike an in-line `CANCEL-SCOPE` (the executing fibre survives and continues past it), an externally-surfaced job failure has no "next instruction" to fall through to. The instance is left exactly as it was at scope-open — "so it can simply be re-run" — re-running is an external decision against the now-clean state, not something the kernel initiates. **Amended (implemented ahead of write-up, folded into §18 v0.10's A18 ratification, ruling G):** this bullet's own logic assumed *some* other fibre survives the kill to keep the instance running. When the interrupting guard's scope spans the instance's *entire* live fibre set (any single-threaded workflow with an interrupting guard around a failing task — a routine shape, not an edge case), killing the trigger leaves zero live fibres, the instance's `state` untouched at `Running`, and no incident — permanently stuck, with no signal. §18 supplies the case this bullet's reasoning didn't extend to: the trigger fibre is **parked on an incident** instead of killed, only when killing it would empty the instance; the proper-subset case (something else survives) is exactly as this bullet describes, unchanged. Not auto-respawn (no new fibre), not continuation (no fall-through) — §13's two prohibitions above hold; resuming via `ResolveIncident` re-executes the guard's own open instruction, which under §18's `GUARD-R>` split is the mechanism that makes "so it can simply be re-run" concretely true rather than merely stated. Also now targets `GUARD-R>` records specifically, not any interrupting guard (§18 ruling G) — a plain `GUARD>`'s definitive-failure path no longer rolls back at all, since it carries no snapshot to roll back to.

**Scope of this amendment:** A and B are D2 word-semantics clarifications (closing Q2/Q4). C is new — no prior D1–D4 section anticipated an automatic (non-instruction-triggered) concurrency-table mutation, so it is recorded as its own ruling rather than folded into §12's D1 amendments. `ConcurrencyRecord`'s shape gained two new optional fields (B) — this is compatible with V1's frozen record shape in spirit (additive, canonically encoded, `None` for every non-`Guard` kind) but is itself a deliberate, reviewed change to a type V1 called frozen, recorded here for that reason.

---

## 14. Amendment v0.6 (2026-07-22)

**Why this amendment exists:** §13 ruling C, as ratified, explicitly says the automatic-rollback rule applies with "no distinction between `ContractViolation`/`BusinessRejection`/exhausted-`Transient`" — any definitive failure inside an interrupting guard rolls back, full stop. Reviewing that rule's actual consequences (V4.6's blind review, then Adam directly) found this is wrong for two of the three classes, not merely under-implemented: an unmatched `BusinessRejection` is a gap in the *workflow's own route map*, not a machine fault, and rolling back destroys the evidence that a stated business outcome occurred; an exhausted-retry `Transient` is the retry budget's own terminal state and belongs in quarantine with its attempt history intact, not silently erased by a rollback. This supersedes ruling C's "no distinction" clause with **ruling D** below — the underlying mechanics of C (which records retire, how the payload is restored, that the triggering fibre is killed not continued) are unchanged; only which failure classes are eligible for the mechanism changes.

**Ruling D — the failure taxonomy is enumerated, not fall-through.** Of the three classes reaching this boundary (retriable `Transient` and routed `BusinessRejection` are handled earlier and unaffected), exactly one is eligible for §13's automatic-rollback mechanism:

1. **`ContractViolation` — technical fault / corruption.** The only class eligible for automatic rollback, and only when the fibre's innermost armed guard is interrupting (§13's original carve-out for `GUARD-N>` and unguarded fibres is otherwise unchanged). **Narrowed further by §18 v0.10 (ruling G):** "interrupting" here now specifically means `GUARD-R>` — a plain `GUARD>`, though also interrupting, carries no rollback snapshot as of §18 and so cannot participate in this rule at all; its definitive failures fall through to the ordinary incident path (§14 class 2/3 treatment), same as a `GUARD-N>` or unguarded fibre. Repeated identical failures of the same scope should escalate rather than loop forever if re-run externally — attempt-counted outside the reverted state, budget reused from `RetryPolicy` (T8.5) rather than a second backoff mechanism. **Not designed here** — see the open items below.
2. **`BusinessRejection` — unmatched route.** Never rolls back, regardless of guard nesting. Always surfaces as an `Incident` — the workflow's route-map gap is information for whoever fixes the workflow, not something the kernel erases.
3. **`Transient`, exhausted (reached this boundary with `retry: None`).** Never rolls back — `RetryDecision::Exhausted` is a terminal state of the existing retry budget, not a fresh failure for the guard-scope machinery to reinterpret. Always surfaces as an `Incident`, with the attempt history that led to exhaustion preserved on it. **Attempt history is not designed here** — see the open items below.

**Meta-rule (E6, "no success-by-default," mirrored onto the failure taxonomy): no failure class reaches rollback by falling through.** The kernel's rollback-eligibility check is an exhaustive match over `ErrorClass` with no wildcard arm — a future fourth `ErrorClass` variant is a compile error at that match site until deliberately classified into one of the three rows above (or a new row), not a silent inheritor of whatever a wildcard would have done. Enforced by the type system, not by convention or code review alone.

**Open, not resolved here — two implementation gaps ruling D exposes but does not close, both requiring Adam's disposition before they're built:**
- **Attempt history has no channel into the kernel today.** `Command::EffectFailed` carries `retry: Option<JobRetry>` (worker id / claim token / backoff hint only, no attempt count), and `Incident::retry_count` has always been hardcoded to `0` regardless of actual attempts — a pre-existing gap, not introduced by this amendment, but one class 3's "attempt history preserved" depends on closing. Candidates: extend `Command::EffectFailed`'s shape with an attempt count (an ABI-relevant change touching every construction site), or look it up store-side from whatever already runs `RetryPolicy::decision()`. Not decided here.
- **No mechanism exists for class 1's repeated-failure escalation.** Nothing survives a guard-scope rollback outside the reverted `domain_payload` — the guard record itself retires as part of the cascade, so even a counter stored on the `ConcurrencyRecord` is lost on the next re-open. An external actor blindly re-running an identically-failing scope has no kernel-provided signal to stop and escalate to quarantine instead. Candidates: a new `ProcessInstance` field outside the rollback snapshot's scope, an external consumer of `V2ScopeCancelled` events keyed by the guard's static `Addr` (survives across re-opens, unlike `RecordId`), or a store-side counter. Not decided here.

**Scope of this amendment:** ruling D revises §13 ruling C's stated scope (the "no distinction" clause), not its mechanics. The two open items are new architectural questions this revision surfaces, deliberately left open rather than guessed at.

---

## 15. Amendment v0.7 (2026-07-22) — RATIFIED

**Why this amendment exists:** §14 closed the failure-taxonomy fall-through but deliberately left two implementation gaps open rather than guess at them: where an attempt count reaches `Incident`, and where a repeated-failure budget for `ContractViolation` lives outside the reverted rollback state. `EOP-PROP-BPMN-ISA-002-A15` worked both into ratifiable decisions with options, a recommendation, and anticipated rebuttals — the same discipline as D1–D4 and the oracle rulings (§12). Ratified as written, with one point (E) resolved by a concrete probe rather than left conditional.

**Unifying principle.** *Execution state lives in the frame — hashed, replayed, integrity-covered. Operational accounting lives beside it — claim-gated and journaled, never in the hash domain.* Attempt counts and repeated-failure budgets both describe a scope's history and health; neither is consulted by `apply` to decide what the machine does next. Putting either in the frame would make every failed attempt mutate hash-covered state, invent a "fields exempt from rollback" concept with no natural boundary, and — for anything keyed by code position — drag artifact addresses into runtime state, in violation of §4.

**Ruling E — attempt-history channel: `EffectResponse::Failed`/`Command::EffectFailed` gain an `attempt: u32` field, populated by the engine adapter, not by individual workers.**

Resolved by probe, not left conditional: `bpmn-lite-types`'s hash-covered frame (`PersistedSnapshotState`, `Transition`) represents pending effects as a bare `pending_effects: BTreeSet<EffectId>` — no per-effect metadata, no `attempt` field anywhere in the Ring-2 hash domain. The only `attempt` column lives store-side, in Postgres's `workflow_effects` table (`bpmn-lite-store-postgres/migrations/050_workflow_effects.sql`), read back into `ClaimedEffect::attempt()`. Since E4 kernel purity already forecloses a store-side lookup from inside `apply`, and the count is not in the frame `apply` receives either, **E-3 (read it from the `Snapshot`) is unavailable.** This resolves the recommendation's stated condition ("verify first, then E-3 if available, else E-2") to **E-2**.

E-2's populating site is concrete, not hypothetical: `bpmn-lite-engine/src/engine.rs`'s `schedule_transient_effect` is the sole call site invoking `RetryPolicy::decision()`; `effect.attempt()` is already in hand there and is currently discarded on `RetryDecision::Exhausted` rather than threaded into `EffectResponse::Failed`, which flows unchanged into `Command::EffectFailed`. One authoritative source, not N worker call sites each capable of drifting or — worse — a crashed-and-redispatched worker reporting attempt 1 on the fiftieth attempt (E-1, rejected on this basis alone). Wiring `Incident::retry_count` from this field also closes the independent, pre-existing defect of it being hardcoded to `0` regardless of actual attempts.

**On "never in the hash domain" and `Incident::retry_count` (v0.8 clarification — see §16):** `Incident` was already part of the canonical encoding before this ruling, so `retry_count`'s populated value does enter Ring 2 — which reads, out of context, as a direct contradiction of the unifying principle above. It is not: the principle governs the *counter*, not any value ever derived from it. The counter ticks store-side, once per attempt (`workflow_effects.attempt`), mutating nothing hash-covered. Its terminal value enters the frame exactly once, as a field on an `Incident` — which is genuine execution state, not accounting: an incident is a durable workflow object that changes what happens next (surfaced to operators, drives routing, is itself subject to resolution). The principle is "accounting doesn't *tick* in the frame," not "no accounting-derived value may ever appear in the frame" — a terminal summary recorded once, as part of a legitimate execution object, is not the thing the principle forbids.

**Same reasoning extends to §18 v0.10's new hash-covered fields, stated once here so it isn't re-litigated per field.** `GUARD-R>`'s rollback-set fields (`rollback_flags`, `rollback_join_expected`, `rollback_session_stack`, alongside the pre-existing `rollback_domain_payload`/`hash`) and `ProcessState::Incidented` are all genuine execution state by the same test applied above — they are read by `apply` to decide what the machine does next (a rollback restores from them; `ResolveIncident` transitions out of `Incidented`), not accounting *about* execution recorded on the side. None of them tick per attempt; each is written once, at scope-open or incident-raise, and read (not incremented) thereafter. Nothing about §18 tests the boundary this principle draws — it's a clean instance of "genuine execution state," not a new edge case.

**Ruling F — repeated-failure budget: a store-side counter keyed `(tenant_id, instance_id, guard_addr)`, checked and enforced at claim time.**

F-3, as recommended. `RecordId` does not survive a guard's re-open (the record retires with the rollback cascade); only the guard's static `Addr` does, so any durable key is `(instance, guard Addr)` or coarser. F-1 (a `ProcessInstance` field) is rejected: it places an artifact address inside the frame, which §4 forbids as written, and would require a ratified carve-out for "diagnostic metadata exempt from rollback" — a category that, once it exists, accretes. F-2 (an external `V2ScopeCancelled`-event consumer) is rejected as advisory-only: it can observe a scope's fiftieth failure but cannot refuse the fifty-first, which is the weak form of the guarantee for a fail-stop system. F-3 is chosen because it is enforcing, not observing, and because it extends T10.3's existing claim-time "should this instance run" gate rather than inventing a second gate that can come to disagree with the first — the same category as the lease and the fence, neither of which is in the hash domain either.

**F's scope of refusal, settled explicitly (raised as a wrinkle, not left implicit):** the budget is scoped per-guard (keyed by `Addr`), but T10.3's claim decision is instance-granular. Ratified: **exhausting one guard's budget refuses claims for that instance** (instance-granular refusal, not scope-granular partial admission) — a partial-admission model (ticking fibres outside the exhausted scope while refusing only that scope's re-entry) would require the claim path to reason about which fibres a claim would touch before granting it, which T10.3 does not do today and which this amendment does not propose adding. An instance with one permanently-exhausted guard is, correctly, an instance that needs operator intervention, not partial operation.

**F's sub-questions, ratified as proposed:**
- **Budget shape:** reuses `RetryPolicy`'s *shape* (bounded, deterministic, terminating in escalation) at a distinct granularity and clock (per-scope-re-run, not per-effect-with-backoff) — not the literal `RetryPolicy` type. A second, explicitly distinct policy type, so "attempt 3" is never ambiguous between the two callers.
- **Reset semantics:** the counter resets when the scope closes successfully (`<GUARD`/`<GUARD-N` executes normally). Ratified explicitly per the proposal's own warning: a non-resetting counter is a slow-acting denial of service against long-lived workflows that accumulate unrelated historical failures.
- **Terminal action:** quarantine-and-surface (an `Incident`), consistent with every other exhausted budget in the system — not silent refusal.

**§4 interaction:** E is a `Command`-shape change, not frame state — no interaction. F's counter is store-side, in the same category as the lease/fence/journal — records *about* execution, never consulted *during* execution. **Superseded in part by §16:** landing F surfaced that its key, `(tenant_id, instance_id, guard_addr)`, needs a `guard_addr` that outlives one guard activation, and `RecordId` does not survive a guard's re-open — so `ConcurrencyRecord` needed a new frame-resident field, `opened_at: Option<Addr>`, to carry it. This is frame state, contrary to this paragraph's claim that F needed none. §16 resolves it: not a carve-out (a ratified exception to a rule that otherwise stands), but a correction to §4's own text, which had always permitted exactly this category of `Addr` presence (`handler` predates this ruling) without saying so precisely. See §16 for the full reasoning; `opened_at` is that section's category (iii).

**Scope of this amendment:** closes the two items §14 left open. Unparks task #78. `apply_job_failure` may now be extended to read/write the new channels — not yet done as of this ratification, tracked as follow-on implementation work spanning `bpmn-lite-types` (new `Command`/`EffectResponse` fields), `bpmn-lite-kernel` (`Incident::retry_count` wiring), `bpmn-lite-engine` (populate `attempt` at the one call site), and `bpmn-lite-store-postgres` (new migration for the repeated-failure-budget table, claim-path enforcement). Neither ruling alters §13's rollback mechanics, §14's class eligibility, the D3 integrity rings, or the D4 theorem lists — both concern accounting beside execution, not execution itself.

---

## 16. Amendment v0.8 (2026-07-22)

**Why this amendment exists:** landing ruling F required `ConcurrencyRecord::opened_at: Option<Addr>` — a frame-resident, Ring-2-hashed field carrying an artifact address — which §15's own "§4 interaction" paragraph had said F would not need ("§4 stands unamended"). That was checked, not assumed correct on the strength of the ratification alone: it surfaced that §4's text, "the K-invariants keep artifact identities out of records," was already false the moment it was written — `ConcurrencyRecord::handler: Option<Addr>` has carried an artifact address since V1.2 (§5, `GUARD>`'s handler target) and no K-invariant has ever forbidden it. The document had prohibited something it always did; the actual prohibition, examined honestly, was never about presence.

**What changed:** §4 (Static structure vs. dynamic activation) is revised in place to state the real rule — three categories of `Addr` presence in runtime state, only the second forbidden:

1. **Code reference / jump target** (`handler`) — legitimate, execution-affecting, no alternative encoding exists.
2. **Runtime execution identity — FORBIDDEN.** The worked example is named explicitly for the first time: resolving `FORK`/`JOIN` pairing by static site instead of the inherited `RecordId` handle, which would make concurrent activations of one site indistinguishable and break re-entrancy (V-3, §7). This is what the K-invariants actually guard against.
3. **Static-site identity for accounting that must span activations** (`opened_at`) — a category §4's original text never contemplated, added by this amendment. The same property that disqualifies `Addr` for category 2 — a static site cannot distinguish activations — is what qualifies it for category 3: a repeated-failure budget is deliberately about a site's history *across* re-runs, not about any one activation.

`handler` is retroactively classified as category 1 (no behavior change — it was always sound, only undocumented as such). `opened_at` (§15 v0.7 ruling F) is classified as category 3. §15's "§4 interaction" paragraph is corrected in place to say what actually landed, rather than left standing as a now-false claim next to code that contradicts it.

**A second, related correction lands with this amendment:** §15's unifying principle ("operational accounting… never in the hash domain") reads as contradicted by `Incident::retry_count` — already part of Ring 2's canonical encoding pre-dating this ruling — receiving ruling E's `attempt` value. It is not a contradiction; §15 is amended with one clarifying paragraph (inline, after ruling E's populating-site description) stating the actual boundary: the principle governs the counter's *ticking* (store-side, once per attempt, hash-uncovered), not every value ever derived from it — a terminal summary recorded once as a field on a genuine execution object (`Incident` — durable, routes behavior, is itself resolved) is not what the principle forbids.

**A note on why this is the third such correction, worth recording as practice rather than dropping once fixed:** §4's "artifact addresses never appear in runtime state" was already false via `handler` when first written; "innermost-first" cancellation order was ambiguous between fibre-depth and record-nesting until the oracle forced disambiguation (§3); now §15's "never in the hash domain" reads as contradicted by a field that predates the ruling. Each time the *practice* was right and the *slogan* needed correcting — the principle is stated as a teaching device (memorable, one-line, load-bearing in argument) and then leaned on as a specification, which it is under-specified for. Cheap habit going forward, cited here so it isn't rediscovered at the next principle's expense: when a principle is stated, name one thing it permits that looks like a violation, and one thing it forbids that looks permitted, in the same breath. §4 as revised above now does this (`handler` permitted, FORK/JOIN static-pairing forbidden); §15's addition above does the same for the hash-domain principle.

**Scope of this amendment:** clarifies §4's text, corrects §15's §4-interaction claim to match what ruling F actually required, and adds §15's hash-domain boundary clarification. Does not change any K-invariant's enforcement, any ratified ruling's mechanics, or any code — `opened_at` and `Incident::retry_count` were already landed and tested against the *correct* reading of §4/§15 (both were always sound; only the document's text was the defect) before this amendment made those readings explicit. No carve-out was granted in either case because none was needed once the principles said what they meant.

---

## 17. Amendment v0.9 (2026-07-22) — consolidating; the document goes stable after this

**Why this amendment exists:** a single V5-scoping session surfaced enough small, genuine loose ends — two reviewer questions dischargeable by evidence now on record, one dead lean now a built fact, one closed ruling that had to be reopened and corrected mid-session, exact figures that were previously approximate — that batching them into one consolidating amendment is cheaper than five separate ones, and matches how this document has always preferred to land related findings together (§13, §15). After this, the document is stable: Q5/Gap 1, the one question no repository evidence alone could settle, was put to Adam directly and ratified closed the same day — §10 carried zero open reviewer questions **as of this amendment landing.**

**Correction, same day, after landing:** that stability held for one further V5 implementation step. Q6 (§10) opened during boundary-timer lowering — materially different from Q5/Gap 1 (those were scope deferrals; Q6 is a regression risk if mishandled, since v1 executes boundary timers on synchronous tasks today and v2 has no word covering it). Not folded into this section's body to avoid rewriting a landed amendment's claims after the fact — recorded here instead, in place, so a reader relying on "stable" above sees immediately that it didn't hold.

**Q1 discharged, by evidence rather than a ratified dialogue.** Unlike Q2/Q4 (settled by Adam ruling on a presented question) or Q3 below (settled by what shipped), Q1 asked whether K-2 survives adversarial scrutiny — answered not by argument but by the 5-topology K-invariant property-fuzz corpus (V4.6 remediation) and Ring 3's unconditional shadow-assert on every `apply` call finding zero violations across the full V4 surface. A different discharge mechanism than Q2/Q4, recorded as such rather than folded into the same "ratified" language, since evidence-of-absence-of-counterexample and a reviewed ruling are not the same epistemic act.

**Q3 settled by what was actually built, not decided freshly.** Whole-frame BLAKE3 (not per-fibre row digests) has been `bpmn-lite-store-postgres`'s Ring 1 mechanism since V2.2 — the "current lean" recorded in §10 was already the shipped fact by the time anyone would go looking for it. The projection sub-question (ship a queryable projection in v2, or await demonstrated need) is left genuinely open — nothing forced an answer, no projection has been built or requested.

**§4/§15's inclusive-gateway and dynamic-arity handling — see the plan doc, not restated here.** The full account of the reject-then-corrected-to-defer episode (`bpmn-lite-engine/src/tests.rs`'s `T-IG-1..6`/`T-AUTH-2` found only via full-workspace test run, the false "zero usage" claim, the revert) lives in `EOP-PLAN-BPMN-ISA-002.md` item 5.2a and `EOP-V0-RECONCILIATION-REPORT.md`'s process-lesson note — this document only carries the ratified outcome, already landed in §5's revised deferred-to-v3 bullet: inclusive gateways and parallel multi-instance are one `FORK-DYN` deferral, not two.

**Q5 and Gap 1, merged and closed (same day, ratified in §10 above).** Both asked whether v2 needs dynamic-arity concurrency now — the one question this amendment couldn't answer from repository evidence alone. Adam's ruling: not required in this V&S. Parallel multi-instance and inclusive gateways both defer to v3 `FORK-DYN` as a firm deferral. **This closes every open reviewer question in §10 — the document has zero open items as of this ruling.**

**§15's hash-domain clarification and §4's three-category framework are v0.8 (§16), not v0.9** — already landed in the prior amendment, referenced here only to confirm nothing further changed them this pass.

**`opened_at`'s status, for the record:** fully landed (§15 v0.7 ruling F, code committed `50e7e7d`), fully tested (three `bpmn-lite-store-postgres` tests: exhaustion→quarantine, explicit-cancel exclusion, reset-on-close), and classified under §16's category (iii). Unaffected by §15's instance-granular-refusal ruling ("exhausting one guard's budget refuses claims for that instance, not just that guard") — that ruling governs the *consequence* of exhaustion, not the *key*; `guard_failure_budget` still counts per `(tenant_id, instance_id, guard_addr)`, so `opened_at` is still exactly what keys it. Nothing about it is open.

**§8 corrected to an exact count.** "+~14 words" is now "+16 words" — `bpmn-lite-types::Instr`'s 16 `V2*` variants, counting `GUARD-N>`/`<GUARD-N` and `WAIT-FOR`/`WAIT-UNTIL` as the distinct opcodes they are (§5's table already listed them this way; only §8's summary was stale).

**§9 input #3 (DSL lowering confirmation) marked SATISFIED,** confirmed during V5 entry-gate scoping: every DSL-emitted construct maps onto a D2 word; the reverse (two words with no DSL emitter yet) is spare capacity, not residue.

**Scope of this amendment:** six small corrections and closures, batched, same day. No K-invariant, ratified ruling, D3 integrity ring, or D4 theorem list changes. No new open question is introduced by this amendment itself; Q5/Gap 1 was narrowed from two overlapping questions to one and then ratified closed (not required in this V&S). **A new question, Q6, opened afterward the same day during V5 implementation — see §10 — so this amendment's original "zero open questions" claim held only briefly; it is not retroactively edited, only corrected in place with a note.**

---

## 18. Amendment v0.10 (2026-07-23)

**Why this amendment exists:** the document fell behind the code. Between v0.9 and this amendment, A18 (a guard/rollback re-derivation forced by three defects found in three consecutive implementation passes against §13 ruling B) was designed, reviewed, and fully implemented and committed — with no corresponding V&S ratification. In parallel, four more questions were worked out and ruled on: Cluster B's dynamic-arity deferral was found to be reversible on cheaper terms than the v0.9 ruling assumed, Q6 was ruled, a gap neither D1–D4 nor any prior amendment had named (what happens when a gateway's runtime decision matches none of its declared routes) was found and closed, and parallel multi-instance was evaluated against Camunda 8's shipped semantics and ruled in. Five rulings, batched into one amendment rather than five, matching this document's own established practice (§13, §15, §17) — implementation for everything not yet built follows this amendment, sequenced by dependency, as tracked in `EOP-PLAN-BPMN-ISA-002.md`.

### Ruling G — A18: guard/rollback semantics re-derived for the multi-fibre case (already implemented and committed; this is the ratification, not the design)

**The root error.** §13 ruling B (v0.5) made the rollback snapshot unconditional — *"every `Guard`-kind scope captures a rollback snapshot... a standard lifecycle snapshot, not opt-in."* That coupled two orthogonal concerns: **control disposition** (what happens to the fibres inside a scope when it fires — interrupting unwinds them, non-interrupting spawns a handler beside them) and **data disposition** (whether the instance's payload is restored to its value at scope-open). BPMN's own boundary events have the first and not the second — an interrupting boundary event redirects control to a handler; it does not roll back process variables. Data rollback is this design's own addition, from Q4's `CANCEL-SCOPE` reasoning (§13). Making it unconditional attached a data semantic to every control scope, which is where three separate defects originated across three implementation passes: the missing external re-run mechanism (closed by §13 ruling C's own text, "so it can simply be re-run" — but with no mechanism specifying how, until this ruling); the guard-scope-spans-all-fibres stuck state (§13 ruling C's kill-the-trigger disposition, silently assuming something else survives — closed by this ruling's park-on-incident amendment, already folded into §13 above); and instance-global rollback erasing sibling branches under a scope that may be branch-local (closed below, V-10).

**The model.** Control disposition and data disposition are independent and separately encoded — a third opcode, not a flag, for the same reason `GUARD-N>` is a distinct opcode from `GUARD>` (§5: the verifier must see the distinction without inspecting operands). Rollback implies interrupting — restoring the payload while non-interrupting members keep running against the restored state has no coherent meaning — so the combination space is three opcodes, not four:

| Word | Control | Data |
|---|---|---|
| `GUARD>` | interrupting: unwind members, spawn handler | none (changed — previously carried a snapshot unconditionally) |
| `GUARD-N>` | non-interrupting: spawn handler, members continue | none (unchanged) |
| `GUARD-R>` | interrupting: unwind members, **no handler** | restore snapshot |

**Handler-spawning guards redirect control; they do not delete it — verified against the code, not assumed.** When `GUARD>` fires, the handler fibre inherits the pre-push control stack (V-4), which includes any enclosing barrier handle inherited from a `FORK`. Confirmed by direct reading of `apply_v2_trigger_guard`'s interrupting branch: `handler_stack` is computed as the pre-push prefix of an existing member fibre's control stack — the enclosing ancestors, a `FORK`'s barrier handle among them — and `v2_reconcile_ancestor_membership` is called with `MembershipOp::Add(handler_fiber_id)` for every handle in it, including the barrier. A handler spawned inside a forked branch is therefore already a live, not-yet-arrived barrier member the moment it exists — exactly BPMN's boundary-event semantics: the token doesn't vanish, it moves to the handler path. This was existing, correct behaviour before this ruling, not a change this ruling required.

**Rollback has no continuation, so it deletes rather than redirects — hence V-10.** `GUARD-R>` runs no handler. There is no fibre to inherit barrier membership, so a rollback under a `FORK` removes a barrier member that never arrives (barrier starvation), and separately restores instance-global payload from a scope that may be branch-local, erasing sibling work. Both problems share one precondition and one fix, now V-10 (§7): **a `GUARD-R>` scope must dominate any `FORK` within its extent** — it may contain a complete `FORK`/`JOIN` region, but must not be contained by one. A rollback-capable scope is instance-wide in its data effect, so V-10 forces it to be instance-wide in its control extent. With handler-redirect already correct and V-10 forbidding the only topology that starved a barrier via rollback, barrier starvation becomes **unconstructible by the verifier**, not remediable at runtime — the three runtime remedies once on the table (raise an incident; shrink the barrier's arity; retire-and-cascade) are all moot, and Ring 3's zero-live-fibre and barrier-satisfiability asserts (D3 §6) stand as the running proof that V-10 continues to hold in production, not as the mechanism that makes the class safe.

**A3 — the rollback set.** Because rollback is now opt-in and dominating, "restore to scope-open" is unambiguous rather than approximate. **Restored:** `domain_payload`; business-meaningful `flags`; `join_expected` (its barriers are within the dominated extent by V-10, so they are being torn down by the same rollback anyway). **Preserved, never restored:** loop and retry counters — restoring them would reset the bound V-8 (§7) relies on, letting a failing scope retry unboundedly, exactly the livelock the §14/§15 failure-budget work exists to prevent; their purpose is to accumulate *across* attempts, which restoration defeats. **Session stack:** restored, as execution-shaped state within the dominated extent. General rule: restore what describes the workflow's *data* state; preserve what describes its *attempt history*.

**Consequence — `ProcessState::Incidented`, split from `Failed`.** Implementing the park-on-incident amendment (§13, above) surfaced that `Failed{incident_id}` was doing two jobs under one name: permanently dead, and resumable via `ResolveIncident`. `is_terminal()` classified it terminal ("no further progress possible") while `ResolveIncident` demonstrably revived it — a pre-existing contradiction A18 walked into, not created, but one that had to be resolved before A18's own spanning-case park could set a state that meant what it said. Ratified: two named, exhaustive predicates over `ProcessState`, each written with no wildcard arm — `is_terminal()` (unchanged meaning; false for the new `Incidented`) for the GC/archival question ("is this done forever?"), and a new `is_schedulable()` (true only for `Running`; false for `Completed`/`Cancelled`/`Terminated`/`Failed`/`Incidented`/`WaitingOnSubmission`/`WaitingOnInvocation`) for the scheduler-claim question ("should the scheduler attempt this?"). The two questions coincide for every prior state, which is why one boolean sufficed until now; `Incidented` is exactly the state where they diverge (not done, not schedulable), and that divergence is the whole finding. This is the same meta-rule §14's ruling D already established for `ErrorClass` (an exhaustive match with no wildcard, so a future variant is a compile error until deliberately classified) applied a second time, to a second classification — not a new principle, a second discharge of an existing one. Verified, not assumed: a temporarily-added dummy `ProcessState` variant produced `E0004` (non-exhaustive patterns) at both predicates before being reverted, confirming the compile-error property actually holds, including through `is_schedulable()`'s grouped match arms.

**Costs, honestly recorded, and what would overturn V-10.** Two more opcodes; one more verifier theorem; a lowering decision per boundary-event kind (ruling I, below, and Part 2 item e of the implementation sequence); and a real restriction — a data-rolling-back error handler cannot be written on a task inside a parallel branch. The argument that this is acceptable is that BPMN never offered that in the first place; boundary events route control, they do not restore data. **This is the one point in this ruling not settled by evidence in this repository:** if a live runbook genuinely needs branch-local data rollback, this model is wrong and the barrier-starvation question reopens with all three runtime remedies live again. No such runbook exists in this repository (`bpmn-lite`'s own tree has no runbook corpus — actual runbooks live in ob-poc or wherever SemOS's production definitions are authored); this ruling proceeds on the domain judgement that BPMN's own semantics make the restriction sound, not on repository evidence that closes the question the way Q5/Gap 1 originally needed closing.

### Ruling H — Cluster B: dynamic-arity concurrency, Design B (`FORK-DYN`)

**The reversal.** v0.9/§17 ratified dynamic-arity concurrency as *not required in this V&S*, a firm (not conditional) deferral to v3, reasoning that supporting it required either combination-enumeration (inclusive gateways, preserving V-3 but not solving the join side's dynamic arity in general) or breaking V-3's static-arity theorem outright (parallel MI) — both expensive enough to defer. That reasoning is superseded, not wrong at the time it was made: the option below wasn't evaluated yet.

**Design B.** Arity is derived as the graph is traversed, not enumerated at compile time — an inclusive gateway's actual arity is however many of its conditions are true at runtime; a multi-instance activity's actual arity is its input collection's length at runtime. Enumeration is out (it doesn't scale and doesn't apply to MI at all); deriving arity dynamically is what both constructs already need, expressed directly instead of worked around. This is cheaper than the v0.9 deferral assumed because **the upper bound is still static** — a gateway's out-degree `n`, or an MI activity's declared cap (ruling K).

**Correction, Part 2 item (a)/(c) implementation (landed 2026-07-23) — the paragraph below through v0.10 mis-described the mechanism and is superseded in place, not merely fulfilled by coincidence.** The original text here said V-3 "weakens from an equality proof (`arity == n`) to a bound (`arity ≤ n`)" and that "`n` on the operand stack is now runtime-computed rather than compile-time-fixed." Built and checked against real code, not re-derived from this prose: `Instr::V2Fork` has no operand stack involvement at all — `targets: Box<[Addr]>` is, and remains, a compile-time-fixed array; the kernel's `arity = count = targets.len()` is set unconditionally from it. **What "dynamic arity" actually is: a compiler lowering pattern, not a kernel or verifier mechanism change.** The compiler always emits a `V2Fork` whose `targets` array has exactly the statically-declared maximum `n`; a branch that doesn't apply at runtime (a false gateway condition, an MI index past the actual collection length) is lowered to jump straight to the shared `V2Join` using only pre-existing opcodes (`BrIf`/`Jump`) instead of doing its nominal work — it still arrives at `V2Join` like every other branch, so the barrier retires via the completely unmodified join mechanism. Consequence: **V-3 does not weaken at all, mechanically** — `targets.len()` already *is* the static bound `n`, by construction, not a separate runtime-computed quantity V-3 needs to compare against a bound. `VerifiedLimits.max_fibers` (computed from `targets.len()`) needed no fix for the identical reason — it was always reading the static bound directly. K-3 (§7, barrier soundness) is unchanged in substance, exactly as this paragraph originally said, but now for the right reason: `arity = count = targets.len()` at `FORK` time was never anything but the static bound, for the fixed-arity case or this one. V-1 and V-2 (bracketing, proper nesting) are untouched — they say nothing about arity. V-10 (ruling G) is untouched by this ruling and it by V-10 — the two are orthogonal (V-10 constrains where `GUARD-R>` may sit relative to `FORK`; ruling H constrains what `FORK n`'s `n` is allowed to be). Proof: `bpmn-lite-kernel/src/lib.rs`'s `v2fork_mixed_real_work_and_skip_to_join_branches_retires_barrier_via_unmodified_mechanism` and `bpmn-lite-types/src/artifact.rs`'s `max_fibers_already_accounts_for_a_v2fork_with_a_skip_to_join_branch` — both pass against the mechanism completely unmodified from before this ruling.

**Note on V3.5's residual.** V3.5 (task #59, closed) left a residual risk that the dual-stack abstract-interpretation core might harbor "another wrong-but-self-consistent computed quantity of V-7's shape" (per the plan doc's V3.5 remediation narrowing). This ruling reopens V-3 narrowly — the arity bound specifically — not the abstract-interpretation core wholesale; V-7's own fix (max_records/max_barriers undercount, V3 remediation) is unaffected and this ruling does not revisit it.

**Ratified: dynamic-arity concurrency is IN for v2.** Parallel multi-instance and inclusive gateways both build on `FORK n`'s widened arity; §5's word inventory and "deferred to v3" list are corrected in place above. See ruling K for parallel MI's own further deltas beyond the shared `FORK-DYN` mechanism.

### Ruling I — Q6: guards carry an arming trigger, timer as the first kind

**Ratified.** A boundary timer *is* a guard with a deadline — the guard doesn't care whether the work inside is synchronous (`ExecNative`/`ExecFfi`), async/effect-based (`AWAIT-EFFECT`), or a nested subprocess (a `FORK` region). This retroactively answers the EX oracle's own §4 open item 1 (the exact `Command` shape for triggering an armed guard, deferred at V4 entry and never actually settled): the arming trigger, evaluated by the scheduler/timer machinery exactly as `WAIT-FOR`'s deterministic timer-ID mechanism (§5) already is, is what issues `Command::V2TriggerGuard`.

**Rejected alternatives, recorded for why, not just that.** A fourth `V2RaceArm::Internal` variant (racing a synchronous job's own completion, mirroring v1's `WaitArm::Internal{JOB_COMPLETE}` mechanism) sits badly against V-5 (§7): "arms are registrations... no unguarded state mutation between arm and park" — a job dispatch is not a registration, it's an imperative action, and forcing it into the race-arm shape would be encoding a category error to avoid a design decision. Treating service tasks as effects uniformly (so every synchronous dispatch becomes `AWAIT-EFFECT` under the hood, and a boundary timer is then just an ordinary race between the effect and a timer arm) is arguably the most architecturally correct answer — it would make boundary timers a special case of the existing race machinery, not a new mechanism — but it is a large scope expansion (every `ServiceTask`/`FfiServiceTask` lowering changes, not just the boundary-timer case), disproportionate to what this question asked. The guard-arming-trigger design gets the same outcome (a boundary timer is just a guard scope wrapping arbitrary work, indifferent to what's inside) without that expansion.

**Not pinned here:** the exact opcode/operand/`ConcurrencyRecord`-field shape of the arming trigger is Part 2 implementation work (§18's own sequencing, item b, below) — this ruling settles the *design*, not the bytecode. §8's word-count table is annotated accordingly.

### Ruling J — zero-match: a compile-time theorem and a runtime rule, not one mechanism doing both jobs

**The gap.** No prior D1–D4 section, nor any amendment through v0.9, addressed what happens when a gateway's runtime decision (an inclusive or exclusive gateway's condition evaluation) matches none of its declared output routes. Ruling H's dynamic-arity work made this concrete: an inclusive gateway with dynamic arity can legitimately compute zero true conditions, and nothing in the theorem list or the runtime word set said what that means.

**Compile-time — new theorem V-11 (§7).** Every instruction must have a path to a terminal instruction (`END`/`END-TERMINATE`) — the dual of the existing start-reachability walk (V-1/V-2's bracketing analysis already walks forward from `START`; V-11 is the same walk's obligation in the other direction). The compiler rejects any artifact containing an instruction with no path to any terminal — an unreachable-end-state node is a compile-time defect, not something that surfaces only when a workflow happens to execute that path. **The V-8 interaction, flagged explicitly because it is easy to get backwards:** V-8 rejects backward edges (loop back-edges) unconditionally, so a loop whose only exit is conditional still needs a forward path to a terminal, and V-11's walk must treat the loop-exit edge as an ordinary forward path, not as a special case — the back-edge is already invisible to V-11 (V-8 forbade it as a *forward* path, which is the only kind V-11's walk follows), and the exit edge is exactly the path V-11 requires. Getting this backwards (treating the back-edge as V-11's proof of reachability) would make an infinite loop with no exit look terminal-reachable when it isn't.

**Runtime — zero-match raises an incident, not a termination.** §14 ruling D already decided this category in substance, for a materially identical shape: an unmatched route is a route-map gap — information for whoever fixes the workflow, not a machine fault and not the workflow's own decision to stop. Applying that same reasoning here: a gateway that evaluates to zero true conditions at runtime raises an `Incident` carrying the reason (which gateway, which instance), using the existing `Incident` mechanism unchanged — **no new `ProcessState` variant, no new field.** It stays resumable, exactly like any other incident: an operator amends the payload (or the underlying data driving the condition), calls `ResolveIncident`, and the gateway re-evaluates. The distinction worth stating plainly, since it's easy to blur: **`END-TERMINATE` means the workflow decided to stop; an incident means the workflow couldn't decide.** Zero-match is definitionally the second, never the first — nothing about this ruling introduces a new way for a workflow to terminate itself.

### Ruling K — parallel multi-instance: IN for v2

**Ratified**, verified against Camunda 8's shipped semantics (parallel is the default MI mode there, not an exotic option). Camunda's model — a multi-instance *body* wrapping an *inner activity*, one instance per `inputCollection` element, the body completing when all inner instances do — maps directly onto this ISA's existing primitives: the barrier activation record (ruling H's dynamic arity) plus forked fibres, one per collection element, exactly as `FORK n` already creates n fibres for a fixed n. An interrupting boundary event over the whole MI construct is simply `GUARD>` (or `GUARD-R>`, if it needs to roll back) wrapping the fork region — already fully covered by V-10 (ruling G); no new mechanism needed for that case.

**Three deltas from Zeebe/Camunda, recorded explicitly because each is a deliberate divergence, not an oversight:**

(a) **An artifact-declared maximum per MI activity is required.** `VerifiedLimits.max_fibers` (ruling H) must be statically computable, and Zeebe has no such ceiling — this is a forced deviation, not a feature parity gap glossed over. Exceeding the declared max at runtime is a typed error, not a silent truncation or an unbounded fork.

(b) **`completionCondition` (early termination of an MI body with cancellation of still-live instances) is deferred**, not ruled in with the rest of MI. It layers a race semantic over a barrier — cancel the remaining instances the moment the completion condition is met, rather than waiting for the barrier's natural zero — which is a genuinely separate design question from "does MI exist at all," and answering it well likely wants the same care V-10 got, not a rushed extension riding on this ruling's coattails.

(c) **An empty `inputCollection` is legal and completes the MI body immediately** (Camunda semantics) — MI arity 0 is not an error. This is stated in deliberate contrast to zero-match (ruling J), where zero matched routes *is* an incident: the two rules are **not unified**, on purpose. A gateway with zero true conditions has failed to route a token that exists; an MI body over an empty collection has correctly determined there is no work to do — the empty case is the *expected* outcome of a legitimate input, not a routing failure. Conflating them because both involve a runtime zero would be exactly the kind of surface-level pattern-matching this document's own practice (§16's "name what a principle permits that looks like a violation, and what it forbids that looks permitted") warns against.

**Correction (ruling K Part 2, landed 2026-07-23 — per-element value access).** The item-(d) XML-frontend-lowering landing (`EOP-PLAN-BPMN-ISA-002.md`'s ruling K entry) built MI's arity/skip scaffolding only, deliberately scoped down: "this codebase has no array/collection-valued `Value`... MI's `inputCollection` is represented by its RUNTIME LENGTH ONLY... per-element VALUE access into the inner activity's own task body is NOT built by this step." That gap is closed here, and it was the more consequential half of ruling K, not a follow-on nicety — Adam's own framing: *"'Do this N times' isn't multi-instance; it's a loop... Without value access, MI as shipped can't express the case that motivated including it [each signature request goes to a specific director]."* Two additions, both CAREFUL-tier, blind-reviewed separately from this ratification:
- **`bpmn-lite-types::types::Value` gains an `Array(Vec<Value>)` variant** — canonical-encoded (tag `0x04`), nesting structurally permitted (not forbidden — forbidding it would require breaking the D3 canonical module's "every encode is infallible" invariant, or an unenforced convention CLAUDE.md's "no trap doors" rejects), bounded by new `MAX_VALUE_ARRAY_LEN`/`MAX_VALUE_ARRAY_DEPTH` constants enforced both at canonical-decode time (mid-recursion, preventing decoder stack overflow on adversarial depth) and at the kernel's existing `validate_snapshot_limits` runtime boundary (extended to walk `Value`s in `fiber.stack`/`fiber.regs`/`instance.flags`, not merely count slots — the "frame size proportional to live tokens" guarantee, §2 glossary, otherwise had a hole once one slot could hold an unboundedly large tree). By-value, per Adam's explicit ruling on this exact question (elements are cloned into each branch's own delivery flag, not referenced).
- **`Instr::V2MiIndexLive`/`V2MiArityCheck`'s `length_flag` field is renamed `collection_flag` and now reads a `Value::Array` directly** (length is `.len()`, derived — not tracked as a separate `I64` alongside it, which would have been a redundant-state footgun). A new instruction, **`Instr::V2MiLoadElement { collection_flag, index }` (`( -- value )`)**, runs on the live path immediately after `V2MiIndexLive`'s `BrIfNot`, pushing a clone of that branch's own element; the compiled per-branch sequence composes it with the pre-existing `StoreFlag` into a synthesized per-branch-unique flag (`<node_id>_mi_element_<i>`), which flows to the inner activity's job through the SAME `orch_flags` snapshot pipeline every other service task's inputs already use — not a new delivery mechanism. A real, checked finding drove this shape: `Instr::ExecNative`'s `argc` field, the "obvious" place to wire an operand into the job, is vestigial (set to `0` at every call site in this codebase, never consumed by the kernel handler); building real `argc` support would have widened this landing's scope to a mechanism every service task shares. `V2Fork` itself needed no shape change, confirmed again — this addition composes existing per-branch straight-line opcodes, exactly as ruling H's own precedent predicted. Full design record, every judgment call, and receipts: `EOP-PLAN-BPMN-ISA-002.md`'s ruling K Part 2 entry.

§8's word count (below) and the "+24 words" instruction-set row are corrected in place to add `V2MiLoadElement` as the 25th (still non-ratified-D2-vocabulary, V5-lowering-internal glue, same class as its two siblings).

**Further correction (post-close, landed 2026-07-23 — gRPC-boundary `orch_flags` array-limit + `Cancel`/`Terminate` zombie-instance remediation).** The `MAX_VALUE_ARRAY_LEN`/`MAX_VALUE_ARRAY_DEPTH` enforcement described two paragraphs above ("at canonical-decode time... and at the kernel's existing `validate_snapshot_limits` runtime boundary") was, an independent blind review found and Adam verified by direct code inspection, incomplete in a way that mattered: `orch_flags` (the actual gRPC client-supplied path that seeds `instance.flags` at `start_process`/`complete_job`) had NO array-limit check at all at the request boundary, and `validate_snapshot_limits` ran unconditionally ahead of every kernel command including `Command::Cancel`/`Command::Terminate` — so an oversized/deep array that reached `instance.flags` left the affected instance permanently unreachable by any command, with no exposed remedy (not `Incidented`, so `ResolveIncident` did not apply). Fixed, not merely noted: `bpmn-lite-server/src/grpc.rs`'s `check_orch_flags` now recursively validates every `ProtoValue`/`ArrayValue` against the SAME `MAX_VALUE_ARRAY_LEN`/`MAX_VALUE_ARRAY_DEPTH` constants before `orch_flags` is ever converted to a `Value`/merged into `instance.flags`; `bpmn-lite-kernel/src/lib.rs`'s `apply` now exempts exactly `Command::Cancel`/`Command::Terminate` from the array-size/depth portion of `validate_snapshot_limits` (fiber-count/stack-length/register-count checks still run for them), so an already-poisoned instance remains reachable by both. Full design record, safety argument for the `Cancel`/`Terminate` exemption's scope, and receipts: `EOP-PLAN-BPMN-ISA-002.md`'s "gRPC-boundary `orch_flags` array-limit + `Cancel`/`Terminate` zombie-instance remediation" post-close entry.

### Scope of this amendment

Five rulings (G–K), continuing the global ruling-letter sequence from §13/§14/§15 (A–C, D, E–F). No K-invariant is changed in enforcement (K-3's substance is explicitly unchanged, ruling H). No D3 integrity ring is added; Ring 3's existing "unconditional structural assert" category (D3 §6) already covers the zero-live-fibre and barrier-satisfiability asserts ruling G's implementation added — recorded as instances of an existing category, not a new ring. §10 has zero open reviewer questions again as of this amendment (Q5/Gap 1 reopened and re-closed differently; Q6 ratified) — stated with the same caution §17's identical claim earned: it held for less than a day once already. Corrections landed at point of relevance: §5 (D2 word table — `GUARD-R>` row, `FORK n`'s dynamic arity, the deferred-to-v3 list), §7 (V-3 weakened, V-10 and V-11 added), §8 (word count, new `ConcurrencyRecord` fields, new `ProcessState` variant), §9 (fixture-corpus note), §10 (Q3's projection sub-question ratified, Q5/Gap 1 reopened-and-reclosed, Q6 closed), §13 (ruling B and ruling C's kill-disposition bullet marked superseded in part), §14 (ruling D's "interrupting guard" narrowed to `GUARD-R>`), §15 (the tick-vs-terminal-value principle extended to this amendment's new hash-covered fields). Implementation for what §18 ratifies but does not yet build (V-11, ruling I's opcode shape, ruling H's `FORK`/`VerifiedLimits` wiring, ruling K's MI lowering, and all frontend lowering) is sequenced by dependency in `EOP-PLAN-BPMN-ISA-002.md`, not built by this amendment — this document records what's ratified, the plan doc tracks what's landed.

---

## 19. Amendment v0.11 (2026-07-24) — Direction A: nesting-aware fork/join pairing

**Ratified by Adam, over a topology-restriction alternative.** V5's post-close loose-end sweep found the compiler's fork/join pairing mechanism (both `GatewayAnd` and `GatewayInclusive`) computed fork↔join identity via a single stack walked in `lower()`'s plain BFS traversal order — wrong whenever multiple pairs are open simultaneously in sibling branches of different lengths, since BFS discovery/finish order doesn't track true graph nesting. Confirmed by hand construction: a two-independently-nested-pair topology mispaired an inner `V2Join`'s `pairing` field to the WRONG sibling branch's `V2Fork` address, silently baked into bytecode. This affects `GatewayAnd` — load-bearing, shipped since V1 — not merely `GatewayInclusive`'s newer v2 lowering.

**Why the verifier alone cannot be the fix (Adam's ruling, verbatim):** *"the verifier can't be the safety net, because lowering produces the annotations the verifier checks. V-3 validates that a JOIN's pairing matches its FORK's arity — mispair fork A with join B, arities happen to agree, V-3 passes. Self-certifying."* This is a direct, material qualification of **V-3 (§7)** as this document currently states it: V-3's text ("the pairing must reference an actual `V2Fork`") was always the correct *specification* — what was wrong was the compiler's ability to actually GUARANTEE that specification held, since the pairing annotation V-3 checks was itself derived by a mechanism that could silently attach the wrong (but still arity-valid) `V2Fork` address. V-3's own text needs no further amendment beyond this note: the theorem was always sound as stated, the *implementation supplying its input* was not. The fix therefore had to be the pairing-derivation mechanism, not a narrower V-3 or a stricter admission gate — rejected alternative: restrict admitted topology to "only what the old BFS-order stack can pair correctly," which papers over the mechanism rather than fixing it, and would only get more restrictive as future dynamic-arity work (already widening what topologies are in scope, per ruling H) continues.

**The fix.** `lowering.rs`'s pairing computation replaced with a DFS-recursive walk whose stack is CLONED before recursing into each branch — the shape (not the code) of `dsl::rpst::verify_sese_nesting`'s `dfs_walk`, named by Adam as prior art already in this codebase (a different code path — the DSL/plan-graph importer, which validates an already-named split↔join correspondence rather than deriving an unknown one, since `IRNode::GatewayAnd`/`GatewayInclusive` carry no partner-name field the way `dsl::plan::ExecutionNode::Join.split` does). One unified, kind-tagged stack rather than two independent per-kind stacks, since the two gateway kinds can nest inside each other's branches today and a unified stack is what lets a genuine cross-kind crossing hazard be caught at all. `verifier.rs`'s `check_gateway_and_nesting` had the identical BFS-adjacent defect (a single non-cloned stack threaded through its own recursion) — this turned out to be the reason the `GatewayAnd` mispairing had never fired in production: the verifier was accidentally rejecting the exact well-nested topology that would have exposed it, for an unrelated reason. Fixed the same way and extended to cover `GatewayInclusive` (previously `GatewayAnd`-only).

**A second defect surfaced during independent adversarial review, fixed in the same landing:** the clone-per-branch fix, applied naively, made the verifier's own top-level "unclosed diverging gateway" check permanently vacuous (the variable it inspected could structurally never be non-empty again), silently re-admitting a genuinely non-well-nested topology — a fork branch dangling straight to `END` without ever reaching its join — that the OLD, buggy code had caught only as an accidental side effect. This is not a new theorem; V-1/V-2 (§7) already claim to cover exactly this ("no path closes a record opened on a different path without a join point at equal control depth" / control-stack balance at every path). It is a correction to the *implementation* discharging V-1/V-2 for the And/Inclusive gateway case specifically, tracked per-branch (not merely "popped at least once anywhere," which a first attempt found still admitted a fork with one dangling branch alongside one healthy one).

**Scope of this amendment.** No new V-theorem, no new D1–D4 decision. This is a correction to the compiler's discharge of already-stated obligations (V-1, V-2, V-3), recorded as a numbered amendment per this document's own practice ("frozen means changes are deliberate and reviewed") rather than a silent patch, because the defect was load-bearing (shipped since V1) and its correction changes what topology compiles. §7's V-3 entry is annotated in place, pointing here, rather than rewritten — its text was never wrong. Full design record, the rejected alternative's reasoning, the masking-bug discovery, the independent review's two findings and their remediation, and receipts: `EOP-PLAN-BPMN-ISA-002.md`'s "Direction A — nesting-aware fork/join pairing" post-close entry.

---

## 20. Open defects against already-stated theorems (2026-07-24) — NOT amendments, reported not fixed

§18 ruling H's "dynamic-arity concurrency (parallel multi-instance, inclusive gateways) is IN for v2" ratification, and §19's Direction A landing, together unblocked lifting `verifier.rs`'s blanket single-`GatewayInclusive`-pair admission gate — the actual Camunda-8-parity feature Adam ranked "cannot be parked." Building that landing's required end-to-end runtime proof (not merely compile-time admission) surfaced two real, pre-existing defects, **neither introduced by this session's work, neither fixed by it, both independently reproduced twice** (once by the implementing agent, once directly by hand afterward). Recorded here as open items against theorems this document already claims hold, not as new decisions or amendments — no V-theorem or K-theorem text changes.

1. **K-1 violated at runtime.** A `GatewayInclusive` nested inside a `GatewayAnd` branch, resolving with dynamic arity below its declared branch count, corrupts kernel concurrency-table bookkeeping — `Ring 3 runtime integrity violation: K-1 violated: record <id> (armed) has member <id>, no live fibre`. K-1 (§7: "every member of every live concurrency record references a live fibre") is stated as inductively preserved by every `apply` word and property-tested; this is a live counterexample the existing fuzz/property corpus did not catch, at the specific AND-fork/inclusive-dynamic-arity interaction.
2. **A basic, common topology fails to compile through the real pipeline.** `lowering.rs`'s bytecode-address layout (`topo_order`, plain BFS) can place a shared `GatewayAnd` join's address earlier than a longer sibling branch's own instructions, tripping `verify_bytecode`'s backward-jump rejection — reproduced with the simplest possible case (a bare parallel gateway, one 3-task branch, one 1-task branch, no inclusive gateway, no multiple pairs, compiled via the real `engine.compile` XML frontend). This is not a multi-pair or dynamic-arity-specific defect; it is a pre-existing gap in ordinary `GatewayAnd` support that the compiler-level test corpus never caught because those tests call `lowering::lower` directly, bypassing `verify_bytecode`.

Both are genuinely separable from the gate-lift itself (the sequential-pairs case is proven correct end-to-end; only nested-under-AND topology hits (1), and (2) doesn't require inclusive gateways at all). Full reproduction detail, receipts, and the specific fixtures: `EOP-PLAN-BPMN-ISA-002.md`'s "multiple `GatewayInclusive` pairs per process" post-close entry. Triage and fix priority is Adam's call, not made here.

---

## 21. §20 item 2 addressed; K-1 scope broadened; a NEW related defect found — OPEN FORK, not decided here (2026-07-24)

Per Adam's explicit ruling ("#2 first, now, not V6" — the address-layout bug is the same root cause as Direction A's pairing bug, BFS traversal order used as a proxy for structure, and fixing it first de-risks everything downstream), §20 item 2 was addressed directly. **§20 item 1 (K-1) was explicitly deferred** ("worth re-running the reproduction once #2 and A land... it may be a symptom rather than a separate defect") — re-run below; NOT redesigned or fixed, per instruction.

**The fix.** `lowering.rs`'s `topo_order()` (plain BFS, used to both assign bytecode `Addr`s and drive instruction-emission order) replaced with a structured, region-aware traversal: `compute_region_map`/`region_dfs` (a DFS walk, clone-stack-per-branch, structurally the same shape as Direction A's `gateway_pairing_dfs` but generalized from gateway-kind-tagged matching to plain out-degree/in-degree, so it covers `GatewayXor` merges too — this IR has no dedicated "converging XOR" node, two XOR branches can reconverge directly on a plain `ServiceTask`, confirmed exposed to the identical hazard) producing a diverging→merge map, and `structured_order`/`layout_region` (lays out each branch's full body contiguously up to its region's merge, the merge once, then continues) consuming it to produce the final address/emission order. §20 item 2's exact minimal repro (bare `GatewayAnd`, unequal branch lengths, no inclusive gateway) now compiles and completes; also proven for 3-way branching and the XOR-merge case. Confirmed separable from Direction A's pairing fix: `compute_gateway_pairing` only converts already-resolved `NodeIndex` pairs to `Addr` via `node_addr` after that map is fully built, so it cannot be corrupted by reordering what produces `node_addr` — verified both by reading the code and by re-running Direction A's own address-assertion regression tests (`test_two_independently_nested_and_pairs_pair_correctly`, `test_two_independently_nested_inclusive_pairs_pair_correctly`), which still pass with real, non-vacuous address assertions under the new layout.

**§20 item 1 (K-1), re-run as instructed — scope is broader than originally reported.** A second reproduction, built as part of this task's own proof sweep, uses a topology with NO `GatewayInclusive` and NO arity mismatch at all: a plain `GatewayAnd` fork nested inside one branch of another `GatewayAnd` fork, every branch unconditionally completing. This compiles and executes correctly through several rounds — direct evidence the address-layout fix itself is not implicated — then hits the identical `Ring 3 K-1 violated: record <id> (armed) has member <id>, no live fibre` error several ticks in. **This is not a symptom of the address-layout bug** (it was unreachable before that fix — this exact shape could never compile — so this is newly *exercisable*, not newly *introduced*). It broadens §20 item 1's scope from "inclusive-gateway dynamic arity" to "any nested-barrier topology (a `GatewayAnd` fork inside a `GatewayAnd` branch), full stop." Captured as `bpmn-lite-engine/src/tests.rs`'s `t_and_v2_nested_gateway_inside_branch_compiles_and_completes`, `#[ignore]`d with the finding recorded in its own doc comment. Per Adam's instruction, not redesigned or fixed here — this is a report of broadened scope, awaiting further instruction, same as §20 item 1 itself.

**A NEW, distinct defect found by adversarial review of this fix, independently reproduced — presented as an open fork, not decided.** `region_dfs`'s in-degree check (`in_degree > 1` ⇒ pop-and-record `div → curr` as the region's merge) has no check that `curr` is actually where *all* of `div`'s own branches converge — it fires on ANY node with static in-degree > 1, including one made multi-predecessor for reasons entirely unrelated to the fork in question (the review's example, independently reproduced with a minimal 9-node hand-built graph bypassing all XML/BoundaryError plumbing, `bpmn-lite-compiler/src/lowering.rs`'s `test_region_dfs_misattributes_merge_to_unrelated_indegree_node`: a `GatewayAnd` fork with branch1 = `t1 → m` and branch2 = `t2 → d → m`, where `d` also receives an unrelated incoming edge — e.g. a boundary-error escalation landing inside a branch, legal BPMN — giving `d` in-degree 2 for reasons unrelated to the fork's own join `m`). Confirmed: `region_map[a]` is recorded as `d`, not `m`, because branch2's DFS arrival at `d` pops and records first, and `or_insert` never lets the correct later arrival at `m` overwrite it. Consequence, confirmed via the full XML→compile path: the layout algorithm bounds branch1 by the wrong merge (`d`, which branch1 never reaches) so branch1 lays out unbounded — including everything after the TRUE join `m` — before branch2 and `d` are laid out, reproducing the exact backward-jump `verify_bytecode` rejection this whole fix exists to eliminate, now on a different, common, legal topology (a boundary-error/other-multi-predecessor node incidentally landing inside a fork's branch) not covered by any of this task's own test fixtures.

This is a compile-time false-rejection of legal, well-nested SESE input — caught by `verify_bytecode`, not silent corruption — but it means the fix resolves the *reported minimal case*, not the general defect class. **This is a genuine open design fork, not a mechanical bug fix, and is not decided in this document.** Two directions were surfaced by the review, neither chosen: (a) validate that a candidate merge node's in-degree is fully and exactly accounted for by paths reachable from every one of `div`'s own branches before recording it (closer to a true dominance/SESE-region check, more expensive, more precise), or (b) make the region-map entry revisable rather than first-write-wins (`or_insert`) with some other structural signal deciding which candidate is correct. Recommendation: (a) — `or_insert`'s first-write-wins semantics is exactly the shape of bug Direction A's own `gateway_pairing_pop` review finding already named and fixed for the *pairing* mechanism (kind-checking before popping); the same discipline applies here, generalized to degree-based matching, and a revisable/last-write-wins entry (b) risks silently trading one wrong answer for a different wrong answer under 3+-way branching rather than actually verifying correctness. Not implemented here per Adam's own working contract ("surface forks; don't decide them") — this fix has enough of the reported bug class resolved (§20 item 2's original repro, 3-way branching, XOR merges) to land as a real improvement over plain BFS, but the misattribution case should be tracked as a known, open, adjacent gap rather than silently absorbed into "done."

**Receipts.** `cargo build --workspace --all-targets --all-features` clean; `cargo test --workspace --no-fail-fast`: `bpmn-lite-compiler` 125 passed (124 + the new misattribution-probe test), `bpmn-lite-engine` 71 passed + 2 ignored (both K-1-related, doc-commented), zero failures workspace-wide; `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. Full design record, the contiguity/XOR/order-independence investigation, the review's full finding set, and the misattribution probe's construction: `EOP-PLAN-BPMN-ISA-002.md`'s "address-layout fix" post-close entry.

---

## 22. §20/§21's K-1 finding: root cause confirmed, fixed, both regression tests un-`#[ignore]`d (2026-07-24)

Per Adam's ruling ("#1 after," #2/Direction A landed first): "worth re-running the `#[ignore]`d reproduction once #2 and A land before designing anything for it; it may be a symptom rather than a separate defect." It was — confirmed by direct instrumentation and reproduction, not by inference, per Adam's own explicit caution against building on an untraced hypothesis ("model-derived hypothesis, not traced — verify before building"). **The literal hypothesis (ancestor-membership deletion scoped only to the retiring record) was NOT what was happening** — the ancestor-removal computation was already correct and complete for every ancestor. The actual root cause is adjacent but distinct: a **stale-read-within-transition** hazard.

**Root cause, confirmed by direct trace instrumentation (temporary `eprintln!`s added at the `check_k_invariants` call site and the `V2Join` cancellation branch, run to a captured failure, removed after diagnosis — not left in the shipped fix).** `apply_tick` runs one fibre across potentially many bytecode instructions within a single transition, without re-snapshotting between them, until that fibre blocks. A nested barrier's survivor can pop an inner `V2Join` — correctly triggering `v2_reconcile_ancestor_membership` to remove the cancelled sibling from the OUTER barrier's membership, staged into this transition's own `changes` — and then, without blocking, immediately execute the OUTER `V2Join` in the SAME transition. That instruction's own record fetch went straight to `snapshot.concurrency_table()` — fixed at the transition's start, blind to the transition's own in-flight writes — retrieving the outer record as it stood BEFORE the reconciliation. Its own routine "not last arrival, decrement count" branch then re-`Insert`s that stale copy, silently overwriting (last-write-wins on `Insert`-by-key) the correct reconciliation moments after it was staged. Confirmed via captured trace: the `Remove` op was correctly computed and correctly staged; the outer record's `members` set still listed the dead fibre in the post-transition committed state anyway.

**Not a new design question — a named kernel remediation against an already-established idiom, exactly as Adam predicted.** `v2_reconcile_ancestor_membership` already carried a "check `changes` for a pending same-transition `Insert` before falling back to `snapshot`" guard (present since V4.2) for precisely this class of hazard — it just wasn't applied at the *other* record-read site (`Instr::V2Join`'s own initial fetch) that turned out to need it too. Fix: `fetch_record_in_transition(snapshot, changes, handle)`, a shared helper generalizing that existing idiom (also handling `Retire`/`Remove` — the pre-existing inline version only checked for `Insert`), now used at both sites. Scoped precisely: audited every other `snapshot.concurrency_table().get(...)` call site in the kernel (5 total) — the other three are either the already-correct pending-aware lookup itself, or single-shot-command handlers (`TimerFired`, guard-scope error-routing walks) provably running with `changes` still empty at the point of the read, per their own doc comments — no hazard, left untouched.

**Both previously-`#[ignore]`d K-1 reproductions un-ignored — same fix resolves both, confirming they were one bug, not two.** `t_ig_v2_two_nested_inclusive_pairs_in_and_branches_route_independently` (§20 item 1's original inclusive-gateway framing) and `t_and_v2_nested_gateway_inside_branch_compiles_and_completes` (§21's broadened, pure-`GatewayAnd`-nested-in-`GatewayAnd` framing) both now pass — **100/100 standalone process runs each, zero failures**, where pre-fix they failed intermittently (~1-in-15 to 1-in-40 runs — an earlier note in this document's own plan-doc entry calling the second test "deterministic" was itself a misread of truncated `cargo test` output, corrected in the test's own doc comment; the actual trigger condition — whether a fibre's continuation after an inner join lands in the SAME transition as the outer join — depends on `BTreeMap`-order-of-derived-UUIDs, which varies by process run, hence the intermittency). Test doc comments rewritten in place to record the confirmed root cause and fix, not left as stale "reported, not fixed" markers.

**Receipts.** `cargo build --workspace --all-targets --all-features` clean; `cargo test --workspace --no-fail-fast` — zero failures workspace-wide, `bpmn-lite-engine` 73/73 (up from 71 + 2 ignored — both un-ignored, both passing); `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. 100 standalone `cargo test ... -- --ignored` runs of each formerly-ignored test, captured pre-fix failure dumps retained as red-state evidence, post-fix: 0/100 and 0/100. Full design record: `EOP-PLAN-BPMN-ISA-002.md`'s post-close entry.

---

## 23. §21's open design fork closed: `compute_region_map`/`compute_gateway_pairing` rebuilt on genuine post-dominance (2026-07-24)

Per Adam's ruling on §21's open fork (the `region_dfs` in-degree misattribution): *"`compute_region_map` should be structurally correct (dominance-based), and pairing, layout, and merge identity all derive from that one map... this is the third face of one root cause"* (BFS-order-as-structure-proxy for pairing and layout, in-degree-as-structure-proxy for merge identity). One correction to Adam's own premise, verified before building on it: `dsl::rpst` (named as prior art computing dominance) does NOT actually compute dominance — it validates an already-NAMED split↔join correspondence (`JoinExecNode.split`), a different mechanism for a different data model (BPMN gateway nodes carry no such partner-name field). Genuine post-dominance was implemented fresh.

**The rebuild.** `compute_post_dominators(graph) -> HashMap<NodeIndex, NodeIndex>` — immediate post-dominator of every node, via the Cooper/Harvey/Kennedy iterative dominance algorithm, computed as ordinary forward dominance over the graph with every edge conceptually reversed (no literal reversed graph built; `Outgoing`/`Incoming` queried in the swapped sense), rooted at a synthetic virtual exit node (added to a local cloned graph, never escapes the function) with an edge from every real sink. A single forward pass over reverse-postorder, no fixed-point iteration, valid on an acyclic graph. `compute_region_map` becomes a thin filter over this one map (every out-degree>1 node, mapped to its post-dominator). `compute_gateway_pairing`'s entire DFS-clone-the-stack mechanism (`gateway_pairing_dfs`/`gateway_pairing_pop`/`GatewayPairKind`, Direction A's own landing) is DELETED, replaced by a linear scan: every diverging `GatewayAnd`/`GatewayInclusive` looks up its own post-dominator directly, recording a pairing only if that post-dominator is a converging node of the matching kind (a cross-kind mismatch silently produces no entry, same as before — `verifier.rs` rejects the hazard structurally, `lower()`'s `ok_or_else` guards fail loudly on any miss). One structural computation now serves all three purposes Adam named, not three independently-maintained mechanisms.

**Regression proof.** All existing pairing/layout tests pass unchanged, including every one of Direction A's own regression fixtures (`test_two_independently_nested_and_pairs_pair_correctly`, `test_two_independently_nested_inclusive_pairs_pair_correctly`, `test_and_pair_with_nested_inclusive_pair_pair_correctly`) — confirming the dominance rewrite is behaviorally correct for pairing, not merely for the region-map identity it was directly aimed at. §21's own misattribution probe (`test_region_dfs_misattributes_merge_to_unrelated_indegree_node`) is updated in place — per its own forward-looking comment — to `test_region_map_resolves_true_merge_past_unrelated_indegree_node`, now asserting the CORRECT answer (`region_map[a] == Some(m)`, not `d`): dominance structurally cannot repeat the misattribution, since the wrongly-attributed node never lies on every path from the fork, so it is never a dominance candidate at all.

**Independent adversarial review (2026-07-24) confirmed the core dominance algorithm correct** (hand-traced against 6 constructed topologies including the direction-swap consistency, the multi-sink/virtual-exit construction, and the exact misattribution scenario) **but found one real, concretely-demonstrated gap**: the doc comment's claim that acyclicity was "confirmed" by "zero `BrCounterLt` emission" was a non-sequitur (that fact concerns a completely different frontend, `dsl/frontend.rs`'s S-expression pipeline) — nothing in this XML→IR pipeline (`parser.rs`, `verifier.rs`) actually rejected a cyclic `IRGraph` before this landing. The reviewer constructed a genuine cycle and confirmed, by direct reproduction against an isolated copy of the algorithm, that the single-pass dominance computation does not fail loudly on a cyclic input — it silently returns a WRONG real node for the affected diverging node, not a safe absence.

**Fixed, not merely documented — per CLAUDE.md's own settled decisions ("Workflow topology is SESE only," "Loops are finitely bounded"), a raw cyclic sequence-flow graph was always illegitimate input for this pipeline, never a legal-but-untested case** (a bounded loop is expressed via `MultiInstance`/`GUARD-TIMER-CYCLE>`, neither of which creates an `IRGraph` back edge). `verifier.rs`'s `verify()` gained a genuine structural check — `petgraph::algo::is_cyclic_directed(graph)` — rejecting any cycle with a clear diagnostic, closing the gap the review found rather than leaving it as an unenforced assumption three downstream functions each independently hoped held. `compute_post_dominators`'s own doc comment corrected to reference this real, enforced guarantee instead of the earlier non-sequitur. New regression test `test_cyclic_graph_rejected` (`verifier.rs`), confirmed red against the pre-fix code by direct review reproduction (not merely reasoned).

**Receipts.** `cargo build --workspace --all-targets --all-features` clean; `cargo test --workspace --no-fail-fast` — `bpmn-lite-compiler` 126/126 (125 + the new cycle-rejection test), `bpmn-lite-engine` 73/73, zero failures workspace-wide, zero ignored; `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean. Full design record, the hand-traced dominance derivation, the review's full finding set, and the cycle-gap fix: `EOP-PLAN-BPMN-ISA-002.md`'s post-close entry.

---

## 24. Adam's post-§23 question answered: no, per-site discipline had already missed two sites — both fixed, structural CI guard added (2026-07-24)

After §23 closed, Adam asked whether `fetch_record_in_transition` is now the ONLY way to read the concurrency table mid-transition, or whether a future edit could reintroduce a raw read — and separately noted the §21 premise correction (`dsl::rpst` doesn't compute dominance) as a second instance of an untraced claim being used as supporting reasoning. Both answered by direct verification against the current source, not by re-asserting confidence.

**The answer to the read-safety question was no, and not hypothetically — two more live instances of the same hazard class already existed, unfixed, before any future edit.** A full audit of every `snapshot.concurrency_table()` call site in `bpmn-lite-kernel/src/lib.rs` found: (1) `v2_rollback_guard_scope`, called from `Instr::V2CancelScope` — which, like the already-fixed `Instr::V2Join`, runs inside `apply_tick`'s own per-instruction loop — still read the raw table directly; (2) `Instr::V2GuardArmTimer`'s `record_kind` lookup hand-rolled its own pending-mutation scan but only checked `Insert`, not `Retire`/`Remove` — the exact narrower-than-needed pattern that caused the original K-1 violation (§22), just not yet triggered. A third, bulk-shaped sibling was also found during the same audit: `Instr::EndTerminate`'s orphaned-record sweep iterated `snapshot.concurrency_table()` directly, so a record opened by an instruction immediately preceding it in the same tick (no intervening park) would be invisible to the sweep and left orphaned forever — precisely the hazard that sweep's own doc comment already says it exists to prevent.

**Fixed.** `v2_rollback_guard_scope` and `V2GuardArmTimer`'s lookup now both route through `fetch_record_in_transition`. `EndTerminate`'s sweep gained a bulk sibling, `armed_record_ids_in_transition`, folding `changes.concurrency_mutations` onto a clone of `snapshot`'s table (same reducer logic `materialize_snapshot` uses) before filtering to `Armed` survivors.

**Structural guard added, not a second round of the same per-site audit that had already missed twice.** New CI lint `scripts/check-transition-read-safety.sh`, matching the established `check-canonical-invariant.sh`/`check-glossary.sh` pattern: forbidden-pattern-with-allowlist, a `--self-test` flag proving it isn't toothless against a deliberate-violation fixture, wired into `layering.yml`. Any function in `bpmn-lite-kernel/src/lib.rs` calling `.concurrency_table()` directly must be in an explicit allowlist — the two sanctioned helpers, plus every function whose safety was traced and proven this session — or CI fails. This converts "a site got missed" from a possibility that depends on someone remembering into a build failure.

**Receipts.** `cargo build --workspace --all-features` clean; `cargo test --workspace --all-features` — zero failures workspace-wide (`bpmn-lite-kernel` 43/43, `bpmn-lite-engine` 73/73, both §22 K-1 regression tests still green); `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean; lint self-test fires against the deliberate-violation fixture, real scan passes clean. Full design record: `EOP-PLAN-BPMN-ISA-002.md`'s post-close entry.

---

## 25. Pre-cutover audit: BoundaryError v2 migration — a real gap found and closed (2026-07-24)

Adam is walking the pre-cutover checklist item by item (MI per-element data access, BoundaryError migration, timeCycle XML lowering, then V6), auditing each rather than trusting its task-tracker "done" status — the same discipline that found real gaps in the dominance rebuild (§23) and the concurrency-read fix (§24). MI per-element's turn found nothing to fix (`Value::Array`'s canonical-encoding path already had depth/length bounds enforced before allocation, both wire-format and runtime-state checks, and existing golden-bytes/proptest coverage). BoundaryError's turn found something real.

**The gap.** Two `BoundaryError` nodes attached to the same host task with the SAME specific error code compile and verify cleanly today. `lowering.rs`'s `push_error_guard_arms` emits both as separate `V2GuardArmError` arms in document order; the kernel's arm handler inserts both into `record.error_routes` (a plain `Vec<(Option<String>, Addr)>`) with no dedup. At runtime, `apply_job_failure`'s route match uses `.find()`, which always returns the first entry for a given code — the second handler is permanently, silently unreachable. No compile error, no verifier error, no runtime diagnostic, no existing test caught it: an authoring mistake (e.g. copy-pasting a boundary error handler and forgetting to change the `errorCode`) fails silently rather than being rejected — exactly the "silent wrong behavior, not a crash" class this session has repeatedly found and closed elsewhere (the region-map misattribution in §21, the stale-read hazards in §22/§24).

**The fix, mirroring an already-established pattern rather than inventing a new one.** `verifier.rs`'s existing 8d check already rejects more than one catch-all (`error_code: None`) `BoundaryError` per host task — 8e generalizes it, tracking `(host, specific code)` pairs and rejecting any pair with more than one `BoundaryError`. `v2_verifier.rs`'s `Instr::V2GuardArmError` arm already had a bytecode-level defense-in-depth duplicate-catch-all check (for hand-assembled fixtures that bypass the IR layer) — extended with the matching specific-code check, scanning the same arming-run address range the catch-all check already scans.

**Not a design fork** — unlike §21's region-map misattribution, there was no ambiguity about the correct behavior once the gap was found: reject the duplicate, exactly as the catch-all case already does. Implemented directly rather than surfaced for a ruling.

**Receipts.** Four new regression tests: `test_boundary_error_duplicate_specific_code_on_same_host_is_rejected`/`test_boundary_error_distinct_specific_codes_on_same_host_is_admitted` (IR level, `verifier.rs`), `guard_arm_error_duplicate_specific_code_rejected`/`guard_arm_error_distinct_specific_codes_admitted` (bytecode level, `v2_verifier.rs`). `cargo build --workspace --all-features` clean; `cargo test --workspace --all-features` — zero failures workspace-wide (`bpmn-lite-compiler` 128/128, `bpmn-lite-types` 94/94); `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean; `check-transition-read-safety.sh` still passes (unaffected by this change, re-run for completeness since `apply_job_failure` is part of both this fix's runtime path and that lint's allowlist). Full audit record, including what was checked and found already-solid: `EOP-PLAN-BPMN-ISA-002.md`'s post-close entry.

---

## 26. Pre-cutover audit: timeCycle XML lowering — a real gap found and closed (2026-07-24)

Third item on Adam's pre-cutover checklist (MI per-element — solid; BoundaryError migration — a real gap, closed in §25). timeCycle's turn also found something real, on the opposite side of the risk this checklist item was named for.

**What's solid.** ISO 8601 `R<n>/<duration>` parsing rejects malformed input, `R0/...`, and counts over 1000, with a typed diagnostic, not a panic. Explicit infinite recurrence (`R/<duration>`, no count) is rejected at parse time — it cannot reach the kernel via XML, so it doesn't conflict with this project's "loops are finitely bounded" decision. `GUARD-TIMER-CYCLE>`'s adjacency to `GUARD-TIMER>` is verifier-enforced at both the IR and bytecode level.

**The gap: the opposite case.** A plain boundary timer with only `timeDuration` (no `timeCycle` at all — the common case, e.g. a reminder on a long-running task) rearmed indefinitely instead of firing once. `V2GuardArmTimer`'s handler defaulted every non-interrupting guard's repeat spec to `remaining: u32::MAX` unconditionally, on the assumption that a following `GUARD-TIMER-CYCLE>` would always narrow it down — but lowering only emits `GUARD-TIMER-CYCLE>` when an actual `timeCycle` was authored. For the ordinary duration-only case, nothing narrows the default, and `apply_timer`'s rearm logic (`remaining() > 1`) reschedules the timer, spawning a fresh escalation handler fibre, for as long as the host task stays outstanding.

Reproduced directly: firing a plain non-cycle boundary timer twice (advancing past the rearmed timer's new due window) hit a `VerifiedLimits` fiber-count rejection — the compiled program was verified for at most 2 concurrent fibres (host + one escalation), and the unbounded rearm tried to spawn a third.

**Fix.** The default changes from `remaining: u32::MAX` to `remaining: 1` (fire once). This has no effect on the legitimate cycle case: `GUARD-TIMER-CYCLE>`'s handler already overwrites `remaining` outright when it runs, rather than narrowing relative to whatever the default was — so the two paths were never actually coupled the way the removed comment assumed.

**Receipts.** New regression test `t_ni_1b_non_cycle_timer_does_not_rearm_after_first_fire` (`bpmn-lite-engine/src/tests.rs`), proven red before green: reverting the fix reproduces the `VerifiedLimits` fiber-count rejection directly, confirming the test catches the real defect rather than a proxy for it. All 6 pre-existing cycle tests (`t_ni_2`/`t_ni_3`/`t_ni_5`/`t_ni_6` and the two related boundary-timer tests) still pass unchanged, confirming the cyclic path is unaffected. `cargo build --workspace --all-features` clean; `cargo test --workspace --all-features` — zero failures workspace-wide (`bpmn-lite-engine` 74/74); `cargo clippy --workspace --all-targets --all-features -- -D warnings` clean; `check-transition-read-safety.sh` re-run, still clean. Full audit record: `EOP-PLAN-BPMN-ISA-002.md`'s post-close entry.

---

## 27. V6.3 — E6 sweep: five silent-fallback defects found and closed across word/lowering/integrity code (2026-07-24)

E6 (EOP meta-rule, "no success-by-default"): no failure class reaches correct handling by falling through — enforced by the type system, not by convention. V6.3 sweeps `unwrap_or`/`unwrap_or_default`/`.ok()` sites across the kernel words, the lowering pipeline, and the integrity/persistence code, classifying each as either a legitimate semantic default (a flag that is genuinely `false` when absent, a counter that is genuinely `0`) or an E6 violation (a fallback that masks an error and produces silently-wrong behavior). Five violations found and fixed; the legitimate defaults left in place.

**Kernel words.**
1. `parse_value_key` (correlation-key decode) handled the `b:`/`i:`/`s:`/`r:` prefixes its own encoder `value_key` produces but not `a:` (`Value::Array`, added in §18 ruling K) — an array-valued correlation key fell through to `Value::Bool(false)`, silently corrupting the key it was buffering/matching on. Fixed to decode the `a:` hex-canonical-bytes case symmetrically. Receipt: `value_key_round_trips_every_value_variant_including_array`.
2. `apply_completion` inserted externally-supplied `orch_flags` keyed by `flag_<u32>`, but a key that failed that format was silently dropped (`strip_prefix(...).and_then(parse).map(insert)` with no else) — a malformed flag from the gRPC/REST job-completion boundary vanished without diagnostic. The kernel-side `apply_completion` is the only place key-format was enforceable (`check_orch_flags` validates count/length but not key format). Changed to return `Result<(), TransitionError>`, rejecting a malformed key with `InvalidCommand`; three call sites propagate via `?`. Receipts: `apply_completion_accepts_a_well_formed_flag_key` / `apply_completion_rejects_a_malformed_flag_key`, both red-before-green.

**Lowering.**
3. Nine `.unwrap_or(Addr::new(0))` sites: a successor node with no assigned address silently lowered to a jump-to-address-0 (a miscompilation into the program entry point), rather than failing. `node_addr` provably covers every graph node (built by `structured_order`'s two-pass dominance traversal), so these are currently unreachable — hardened to `.expect("lowering: successor has no assigned address")` so any future regression that breaks that coverage fails loudly at compile time rather than emitting a corrupt jump.
4. `parse_corr_reg` converted a BPMN `correlationKey` XML attribute to a register index, silently defaulting to register `0` (`unwrap_or(0)`) on any non-numeric input — an authoring mistake (or a named/symbolic key, see the fork below) compiled to "register 0" with no diagnostic. Changed to return `Result<u8>`, rejecting a non-numeric key; three call sites propagate via `?`. Receipt: `parse_corr_reg_accepts_numeric_register_rejects_named_key`. This fix caught a genuine latent bug in `parser.rs`'s `test_full_pipeline_kyc_open_case`, whose fixture used `correlationKey="=case_id"` and had been silently lowering to register 0 undetected (the test only asserts on instruction count, never on correlation matching); the fixture was corrected to a literal register.

**Integrity / persistence.**
5. `commit_transition` (postgres store) read the prior snapshot envelope with `SnapshotEnvelope::decode(bytes).ok()` at two points — the prior concurrency table and the prior state hash. A decode failure on a persisted-but-corrupt row was swallowed: the commit proceeded as if no prior snapshot existed, silently dropping live concurrency records and writing a genesis (`[0u8;32]`) prior-state-hash that breaks the Ring 2 chain. Restructured to decode once and reject the commit with `CommitError::Integrity` on failure (defense-in-depth behind the Ring 1 physical-integrity check that already guards load-before-decode). Same file, `epoch_ms_to_datetime` substituted `Utc::now()` for an out-of-range `epoch_ms` — silently writing the wrong timestamp into a claim/lease/expiry column; changed to panic, since an out-of-range value means the caller computed a corrupt epoch and that must fail the commit.

**Left in place as legitimate defaults** (not E6 violations): flags/counters defaulting to `false`/`0` when a key is genuinely absent; `join_counts.get(...).unwrap_or(0)` (no join arrivals yet = 0); payload-update-or-keep-current in the message-consume path; state-override-or-snapshot; the env-var canonical-sample-rate default; the guard-failure-budget saturating cast (fails *closed* toward exhaustion on an impossible negative); the empty-prior/genesis defaults now guarded by the decode-reject above; `transition.rs`'s jitter-seed `try_into().unwrap_or([0;8])` (provably-unreachable slice of a fixed-size hash, non-integrity retry-timing jitter).

**Open fork surfaced, not decided (per CLAUDE.md "surface forks, don't decide them").** The `parse_corr_reg` fix exposed that named/symbolic correlation-key resolution — mapping a variable name to a register, analogous to `flag_intern` — is entirely unimplemented in the lowering pipeline, despite the parser correctly stripping the Zeebe FEEL-expression `=` prefix (implying it was an intended but never-wired feature). The fix does not invent a name-resolution mechanism; it rejects non-numeric keys honestly. Whether to (a) implement symbolic correlation-key resolution, (b) formally document literal-register-only as the supported surface, or (c) something else, is Adam's call.

**Receipts.** `cargo build --workspace --all-features` clean; `cargo test -p bpmn-lite-compiler -p bpmn-lite-engine -p bpmn-lite-kernel -p bpmn-lite-types --all-features` — zero failures (`bpmn-lite-compiler` 128/128, `bpmn-lite-engine` 74/74, `bpmn-lite-kernel` 46/46, `bpmn-lite-types` 94/94); `cargo clippy -p bpmn-lite-store-postgres --all-targets --all-features -- -D warnings` clean. The `commit_transition` decode-reject and `epoch_ms_to_datetime` panic are DB-bound paths behind the Ring 1 load-guard; verified by construction and full build/clippy rather than a live-DB regression test.

---

## 28. Amendment v0.12 (2026-07-24) — §27's open fork resolved: content-based message correlation (Camunda-8 parity). ISA change to three ratified v2 words.

§27 surfaced that named correlation-key resolution was unimplemented. Investigating the fix uncovered the real defect: the entire correlation *substrate* is vacuous, and the correlation *model* cannot express a dynamic business key.

**What was found (the substrate is dead, and the model is too weak).**
- `corr_reg` on `Instr::V2WaitMsg`/`PublishMessage`/`V2ArmMsg` and `V2RaceArm::Msg` indexes `Fiber::regs: [Value; 8]`. **`Fiber::regs` is written nowhere** — not by the XML frontend, not the DSL frontend, not the kernel. It is initialized to `[Bool(false); 8]` and only ever read by these `corr_reg` sites. Every XML-lowered wait/publish therefore correlates on the constant `value_key(Bool(false)) = "b:false"`: correlation does not discriminate at all today.
- The correlation *model* is intern-scalar, not content-based. The external wire contract (`grpc.rs::proto_to_correlation_value`) requires a string correlation key to arrive as `str_<u32>`/`ref_<u32>` — an **intern id**, assigned at compile time. `Value::Str(u32)`/`Value::Ref(u32)` cannot represent a runtime-dynamic string (there is no runtime intern table). And `assign_storage` routes String/domain data objects to `domain_payload`, wholly outside this intern-scalar model. So a `case_id` arriving in a message payload — the ordinary Camunda business-key case — is inexpressible.

**Ruling (Adam, 2026-07-24): implement content-based correlation, with Camunda-8 message correlation as the minimum feature floor.** This is an ISA change to three ratified, hash-frozen v2 words and a change to the gRPC correlation wire contract — recorded here as a v2 ISA amendment, per the keystone/hash-freeze discipline, before implementation.

**The model (content, not intern id).**
- **Correlation key = a content string, resolved from process data.** At lowering, a `correlationKey` FEEL expression (`=case_id`, `=`-prefix already stripped by the parser) is resolved by the existing `resolve_expression` into a `BindingSource` (`Literal` scalar / `FlagRef` / `DomainPayloadRef`) — the same abstraction the FFI binding layer already uses to turn a named data object into a runtime value. No new name-resolution mechanism.
- **Carriage: a side table `v2_corr_sources: BTreeMap<Addr, BindingSource>`**, keyed by the wait/publish/arm instruction's own address, mirroring `v2_ffi_task_decls` exactly (V-9-compatible — V-9 forbids race-plan/join/boundary *control-flow* tables, not data-binding tables; the `v2_ffi_task_decls` precedent is explicit). The three `Instr` variants and `V2RaceArm::Msg` drop `corr_reg` entirely.
- **Runtime resolution at park/arm/publish:** resolve the `BindingSource` against the instance (`flags` + `domain_payload`) to a scalar, then canonicalize to a correlation string via one shared `correlation_key_string(scalar) -> Result<String>` (bool → `"true"`/`"false"`, i64 → decimal, string → content verbatim; array/object → typed reject, matching the existing "correlation keys must be scalar" boundary). The single shared function is load-bearing: waiter, publisher, and wire must derive the key identically or matching silently fails — no second derivation path may exist.
- **`WaitState::Msg.corr_key: Value` → `String`** (the resolved content key). Matching becomes pure string equality (it already compares against `correlation_key: String`). `V2RaceArm::Msg` likewise stores the resolved content string.
- **Wire contract (gRPC):** `proto_to_correlation_value` accepts raw content (string / i64 / bool) and canonicalizes via the same `correlation_key_string`; the `str_<id>`/`ref_<id>` intern-id requirement is retired. An external publisher sends `correlationKey: "ACME-42"`, as Camunda 8 does.
- **`Fiber::regs` is deleted** (dead storage): the field, its canonical encoding, the `max_registers` artifact metric, and the per-fiber register-count `VerifiedLimits` check. Net kernel/state simplification, serving the V6 "kernel LOC net-negative" gate.

**Camunda-8 correlation scope committed as the floor** (what "done" means): correlation key from a process-variable expression; publish/correlate by message name + content correlation key + payload, with `msgId` dedup and TTL buffering (buffer already exists); subscriptions via intermediate catch / receive task / message boundary / send task; buffering until a subscription appears within TTL. **Explicitly deferred, flagged for Adam:** *message start events* (instantiate-a-new-instance-by-message) — a process-instantiation concern, not part of the wait/publish word surface being changed here; called out as the one boundary of "Camunda-8 minimum," to confirm or pull in.

**Blast radius (implemented as Tranche V7, CAREFUL — blind review at close):** `types.rs` (3 `Instr` + `V2RaceArm::Msg` + `WaitState::Msg` + `v2_corr_sources` metadata); `canonical.rs` (those encodings + `Fiber` sans `regs` + golden bytes + proptest); `artifact.rs` (`max_registers` retired); `ffi_bindings.rs` (`correlation_key_string` + a `resolve_correlation_value`); kernel (three handlers + delivery match + `V2RaceArm::Msg` resolution + `regs` removal); `lowering.rs` (three sites: `parse_corr_reg` → `resolve_expression` into `v2_corr_sources`); `verifier.rs`/`v2_verifier.rs` (a correlation source must resolve to a scalar); `grpc.rs` (wire contract). REST carries no message-publish path (only parent/child `correlation_id`), so it is untouched.

**Receipts (V7, landed 2026-07-24):** implemented across 7.1–7.6. `ffi_bindings::correlation_key_string` is the single content derivation (6 red→green unit tests) reused by waiter, publisher, and the gRPC wire; `encode_ffi_inputs` refactored onto the same resolver so no second derivation path exists. The ISA change dropped `corr_reg`/`Fiber::regs`/`max_registers` and added `v2_corr_sources`; Fiber/ProcessInstance golden bytes regenerated (314→298, the 8 removed register slots). End-to-end parity fixture `bpmn-lite-engine/tests/correlation_content.rs`: two instances park on the same message name, discriminated purely by a dynamic string business key resolved from each payload — a wrong content key does not wake an instance, its own key does (red against the pre-§28 constant-key substrate). Full workspace `cargo test`/`clippy -D warnings` green; `check-transition-read-safety.sh` clean; postgres suite green (its `regs` column left vestigial/empty for the V6.4 schema rebuild). Authorship-blind review of the correlation-model change dispatched at close (CAREFUL tier). Plan post-close entry carries the full receipts.

**Blind review closed (CAREFUL tier):** one HIGH finding — the kernel `V2WaitMsg` buffered-before-wait path consumed `buffered_messages().first()` without matching name+key against the executing fibre, mis-routing a sibling's buffered message under an intra-instance concurrent-wait split — fixed (name+key `find`, red→green regression test), the fix mirroring the arrival-time `apply_message` gate. All other priority items (single-derivation across the four correlation sites, address keying, canonicalization, fail-closed, `Fiber::regs` deletion, golden bytes) independently verified sound. The one deferred Camunda-8 boundary (message start events) stands, confirmed acceptable by Adam.

## 29. Deviation (2026-07-24) — Ring 4 scoped to detect-and-surface; proactive fleet replay descoped in favour of snapshot/PITR recovery

**Supersedes §6 Ring 4's "nightly from-checkpoint replay fleet-wide" clause.** Raised by Adam while scoping V6.1: *why proactive replay at all?* The challenge holds.

**The reasoning.** D3's own doctrine is "detection, not correction — fail-stop, never repair" (§6). Detection is already load-time and cast-iron: the claim/hydrate path verifies Ring 1 (BLAKE3 over raw bytes, pre-decode), canonical-form fixed-point, and Ring 2 (snapshot head ⇔ the *independent* `workflow_journal` hash chain) **before a corrupt frame reaches the deserializer or executes** — including for a parked instance, which is checked on its next claim. A proactive nightly replay adds nothing a snapshot/PITR recovery design does not already have:

- Random bit rot → Ring 1 catches it at load → restore.
- Correlated logical rewrite of snapshot+frame_hash → Ring 2's *separate-table* journal chain catches it at load → restore.
- Corruption of the journal itself → replay cannot help (the journal *is* replay's source of truth); only restore helps.

In every corruption class the recovery is a point-in-time restore, and detection already fires at load. A fleet-wide from-checkpoint sweep is therefore redundant with the recovery design, and its only real cost is unbounded: from-genesis replay is O(journal length) per instance, growing monotonically for the life of a long-lived custody/KYC case.

**Ruling (Adam, 2026-07-24): no fleet-wide proactive scan. On detection, isolate and surface for point-in-time restore.** `replay()` (the from-genesis primitive, test-proven) is **retained as an on-demand forensic tool** for incident investigation, not wired to run automatically. The §6 failure-model row "kernel bug writing inconsistent state → Ring 3 + Ring 4" is served by **Ring 3** (unconditional structural asserts at every park/resume): a bug-written inconsistent frame is caught at the next resume; an instance that never resumes is inert. If proactive scanning is ever wanted, it rides the planned mark-and-sweep GC walk over the wf universe as verify-on-visit, not a separate subsystem — no periodic-checkpoint retention, so no new store surface.

**The one hardening this requires (V6.1 = detect → isolate → surface):** the postgres claim path already quarantines on a load-time `IntegrityError` (sets `quarantine_state='replay_integrity_violation'`, de-leases, returns `ClaimError::Integrity`), but two gaps defeated "surface for PITR": (A) the claim predicate filtered only on lease, so a quarantined instance was re-claimed → re-checked → re-quarantined each tick (fail-closed but not isolated); (B) the claim-path quarantine was a *silent column set* — it did not emit the `InstanceQuarantined` audit event the explicit `quarantine_instance` op emits, so an operator watching the event log never saw it and PITR was never triggered. Both closed: (A) `AND quarantine_state IS NULL` added to the claim predicate (a re-claim of a quarantined instance returns `None`, not churny re-detection); (B) the claim-path quarantine emits the same `InstanceQuarantined` event + notify. Both red→green.

## 30. Implementation status — IMPLEMENTED (with open gates and the consolidated deviations table)

**Status: IMPLEMENTED.** D1–D4 are implemented against receipts and landed on the v2 default across tranches V1–V5 and V7: the D2 word set in `kernel::apply` with K-theorem discharge (V4); the D1 concurrency-record model (records, control stack, dynamic-arity FORK) (V1–V3, §18); the D3 integrity rings 1–3 + 5 wired into the claim/hydrate and park/resume paths (V2, V4.3, V6.1); the D4 proof obligations V-1..V-11 in the abstract-interpreter/verifier over every artifact (V3, §18); canonical tagged-binary encoding with golden-bytes + proptest + cargo-fuzz corpus (V2); and content-based message correlation to Camunda-8 parity (V7/§28). The glossary lint gates the whole workspace (V6.5). This marker is **precise, not blanket** — the following are the standing open gates, each tracked in `EOP-PLAN-BPMN-ISA-002.md`:

- **6.2** — KERNEL-001 standing gates re-run against v2 and the T11 performance-metric suite (open).
- **6.4** — destructive cutover (wipe persisted state, recompile corpus, cold-start readiness) (open; needs explicit go-ahead).
- **6.6** — the guard-failure budget policy surface (`ScopeFailureBudget` is a hardcoded placeholder pending a policy-configuration ruling) (open).

Any change to a row below is itself a V&S amendment, reviewed — the table is normative, not a summary.

### Deviations & deferrals from Camunda 8 / Zeebe / BPMN (consolidated)

| # | Area | Reference (Camunda 8 / Zeebe / BPMN) | ISA-002 v2 | Ratified |
|---|---|---|---|---|
| D1 | Persistence granularity | Instruction-level; a threaded interpreter may stop between any two NEXTs | The **transition**, not the instruction: commit frequency ∝ *waits*, not instruction count | §1 |
| D2 | Version fields | Dispatch/migration keys; migration machinery present | Corruption **tripwires** — each versioned surface accepts exactly one value, else refuses atomically; migration deleted, cutover is wipe-and-recompile | §6 / D3 |
| D3 | Topology & loops | Arbitrary cyclic sequence flows; unbounded while-loops | **SESE-only**; any cyclic `IRGraph` rejected at compile (`is_cyclic_directed`); loops only via finite `MultiInstance` / bounded `GUARD-TIMER-CYCLE>`; infinite recurrence `R/<duration>` rejected | §23, §5 |
| D4 | Guard-boundary failure | v1/Camunda: `Incident` + error routing on definitive job/effect failure | Inside a rollback-capturing interrupting guard (`GUARD-R>`), a definitive **`ContractViolation`** auto-runs `CANCEL-SCOPE` restore-and-unwind (no incident, no routing); `BusinessRejection`/exhausted-`Transient` still surface as an `Incident` | §13→§14→§18 |
| D5 | Multi-instance ceiling | Zeebe imposes no fan-out ceiling | An artifact-declared per-MI maximum is **required** (`VerifiedLimits.max_fibers` must be statically computable); exceeding it at runtime is a typed error, not silent truncation | §18 ruling K(a) |
| D6 | Empty MI collection vs zero-match | (matches Camunda for MI) | Empty `inputCollection` legal, completes the MI body immediately (arity 0 not an error); deliberately **not unified** with a zero-match gateway, which raises an incident | §18 ruling K(c) |
| D7 | Zero-match gateway | (undefined in D1–D4) | Raises a resumable **`Incident`** (never a self-termination): "an incident means the workflow couldn't decide" | §18 ruling J |
| D8 | Ring 4 replay | Proactive from-checkpoint replay/scan | **Descoped** to detect→isolate→surface; recovery is snapshot/PITR; `replay()` retained on-demand forensic only | §29 (supersedes §6 Ring 4) |
| — | *Deferrals (coverage gaps, not behavioral deviations):* | | | |
| F1 | Message start events | Camunda-8 message correlation floor | **Deferred** — a process-instantiation concern outside the wait/publish word surface (confirmed acceptable) | §28 |
| F2 | MI `completionCondition` | Camunda/Zeebe early-termination + cancel-live-instances | **Deferred** — layers a race over a barrier; wants its own design pass | §18 ruling K(b) |
| F3 | Compensation | Reverse-order compensation handlers | **Deferred to v3** — table/stack already carry room (`kind` discriminant, archive-on-retire) | §5 |
| F4 | Event subprocesses | BPMN event subprocesses | **Deferred to v3** — expressible later as instance-root non-interrupting guards | §5 |

The v2-internal supersessions (the V&S overturning its own earlier rulings — unconditional→`GUARD-R>`-scoped rollback, dynamic-arity un-deferral, the `Addr`-in-state three-category rule, the intern-scalar→content correlation cutover, etc.) are the amendment history itself and live in §12–§29, not this table.

## 31. Amendment v0.13 (2026-07-24) — guard-failure budget ceiling is per-guard, artifact-resident (closes V6.6, ruling F's open config surface)

§15 (ruling F, v0.7) ratified the *mechanism* of the repeated-failure escalation budget — a per-`(tenant_id, instance_id, guard_addr)` counter, store-side, checked at claim/commit time, that quarantines an instance after too many automatic `ContractViolation` rollbacks of the same guard — but left the *ceiling* as a hardcoded `ScopeFailureBudget::new(1, 5)` placeholder, explicitly flagged as needing a policy-configuration decision before cutover.

**Ruling (Adam, 2026-07-24): the ceiling's authority lives in the artifact, per-guard.** Each guard MAY declare its own failure budget; the compiler lowers it into a pinned artifact side table; the store reads the ceiling from the loaded artifact per `guard_addr`. A workflow-level default covers un-annotated guards. This is the only option consistent with the thesis — authority in the artifact, compiler binds, version = content hash — over the two rejected alternatives: per-workflow (can't distinguish a flaky external-call boundary from a hard contract check within one workflow) and a plan-level config global (operational config, not artifact authority — it merely launders the magic number and can't express per-guard tolerance).

**The split (unchanged from §15, made explicit): ceiling ≠ counter.**
- **Ceiling (this ruling): artifact-resident, hashed, pinned.** A new side table `v2_guard_budgets: BTreeMap<Addr, ScopeFailureBudget>`, keyed by the guard's `opened_at` address, plus a workflow-level `default_guard_budget: ScopeFailureBudget`, both carried in `ArtifactMetadata` exactly as `v2_corr_sources`/`v2_ffi_task_decls` are (V-9-compatible — a data-binding table, not a control-flow one). Because it rides the artifact envelope, the ceiling is part of the artifact hash: a changed budget is a changed artifact, re-verified and re-pinned, never silently mutated.
- **Counter (§15, unchanged): store-side, operational, outside the hash domain.** The `guard_failure_budget(tenant_id, instance_id, guard_addr, failure_count)` row and its increment/reset stay exactly as ruling F built them. Only the *comparison ceiling* moves from a constant to `v2_guard_budgets.get(guard_addr).unwrap_or(default_guard_budget)`.

**Lowering & authoring.** The per-guard budget is a BPMN boundary-event annotation (a `zeebe:`-namespaced extension attribute on the boundary event, mirroring the existing boundary-timer/loop-characteristics extension pattern — a deliberate, documented Zeebe-superset deviation, as boundary budgets have no Zeebe equivalent); the process-level default is the workflow default. Un-annotated guards inherit the workflow default; a workflow with no default declared inherits a conservative compiled-in default (the retired placeholder's ceiling, 5). The DSL frontend, having no boundary-budget surface yet, emits the default for all guards.

**Verifier.** `max_failures ≥ 1` (already enforced by `ScopeFailureBudget::new`), and every `guard_addr` key in `v2_guard_budgets` must resolve to an actual guard-opening instruction (`V2Guard`/`V2GuardR`/`V2GuardN`) at that address — a budget keyed to a non-guard address is a lowering defect, rejected at admission, not silently ignored.

**Blast radius (implemented as Tranche V8, CAREFUL — blind review at close):** `transition.rs` (`ScopeFailureBudget` already present; no change); `artifact.rs`/`types.rs` (`v2_guard_budgets` + `default_guard_budget` metadata + builder/accessor + legacy round-trip wiring, mirroring `v2_corr_sources`); `lowering.rs` (parse the boundary-event budget annotation + process default → `v2_guard_budgets`); `verifier.rs`/`v2_verifier.rs` (guard_addr-resolves-to-a-guard check); `store_postgres.rs` (`apply_guard_failure_budget` reads the artifact ceiling lazily on the `V2ScopeCancelled` path instead of the `(1, 5)` constant). Receipts: red→green per step; an end-to-end fixture where two guards in one workflow carry different budgets and escalate at their own ceilings; the retired hardcode proven gone.

---

*Ratification of D1–D4 unblocks EOP-EX-BPMN-ISA-002 (oracle) and EOP-PLAN-BPMN-ISA-002 (tranches V1–V6). Nothing in this document alters the KERNEL-001 durability substrate.*
