# EOP-REV-BPMN-DESIGN-003-BASELINE-001 — Code-Facts Register (repo copy)

> Landed 2026-07-25 as the R11 close-out. The reviewed document
> (EOP-VS-BPMN-DESIGN-003 v0.2) is NOT in this repository and was not
> found anywhere on searchable disk; this register is the durable record
> of the validation verdicts until the source doc surfaces, at which
> point its claims must be amended against this register (IDs C1-C6,
> S1-S9, F1-F6). The F-findings were remediated on
> feat/baseline-remediation-001 (R1-R9, commits 38c70d2..38f188f).

# EOP-REV-BPMN-DESIGN-003-BASELINE-001 — Code-Facts Register

## Phase 0 — Baseline pin

| Repo | HEAD |
|---|---|
| bpmn-lite (workspace under review) | `75f7a45a8f041e1359edaefdb709913217b2a1ca` (branch `feat/v4-1-d2-word-set`; tree == `main` @ `85499ed` merge) |
| dsl | `a043e7f3d40262b78b367a6c18ac4a937c7498c6` |
| ob-poc | `0839e877db27f16cafe63c017dbd9942cb71da13` |
| rust (contains `ob-semantic-matcher`) | **NOT a git repository — no commit pinnable** |

Ancestors verified present: `a7e5bf8`, `7cace5e`, `e8e563f`, `02b1fc0` — all ancestors of HEAD. **No HALT.** Build: 0 errors. Tests observed: **1048 passed / 0 failed / 0 ignored**.

## Phase 4 — Register

| ID | Claim | Verdict | Evidence | What the code actually does (if divergent) | DESIGN-003 impact |
|---|---|---|---|---|---|
| C1 | Kernel, verifier, decoder build for wasm32-wasip2 with native/WASM replay-hash equality CI gate | **PARTIAL** | wasm builds: `layering.yml:32-33`, `production-gates.yml:55-56` (`cargo build -p bpmn-lite-kernel --target wasm32-wasip2`). Parity gate: `layering.yml:34-38`, `production-gates.yml:86-91` (`cmp` of native vs wasmtime stdout). Fixture: `bpmn-lite-kernel/src/bin/replay_fixture.rs:35-72`. Comparand: `Transition::canonical_bytes` = `serde_json::to_vec` (`transition.rs:1434-1436`) | Only ONE crate is wasm-built (`bpmn-lite-kernel`); verifier + codec are `bpmn-lite-types` modules transitively compiled in. The gate DOES exercise the verifier (`artifact.rs:234→313→554`) and canonical *encoder*; the canonical *decoder* is compiled-in but not exercised. The compared bytes are **serde_json** output, not the Ring-2 canonical codec (see F1) | §0 C1 — module-boundary phrasing wrong; parity semantics narrower than claimed |
| C2 | `dsl::rpst::verify_sese_nesting` is real RPST, reusable for Designer-side region/pairing construction | **REFUTED** | Lives at `bpmn-lite-compiler/src/dsl/rpst.rs:6` (internal `crate::dsl` module) — **zero hits in the `/dev/dsl` repo**. Algorithm: DFS, clone-stack per branch (`rpst.rs:68-69,91-92`), pops against pre-named `JoinExecNode.split` (`rpst.rs:44,74-75`); returns `Ok(())`/`String` — no fragment tree, no triconnected decomposition. Consumers: `plan.rs:49`, `closure.rs:144`, `bpmn-lite-authoring/src/importer.rs:6,318` | It is a nesting *validator* over pre-named split/join partners, not RPST, and cannot *construct* pairings (IR gateways carry no partner-name field). The actual pairing oracle at HEAD is `compute_post_dominators`/`compute_gateway_pairing` (`lowering.rs:967,1259`, doc `:1231-1244` "no stack, no DFS") — both **private**; crate exports only `lower`/`lower_v2` (`lib.rs:24`). Correct shared oracle = the post-dominator computation, currently unexposed | §0 C2 — reuse premise false; Designer-side pairing needs the (private) post-dominator oracle exposed, not `rpst` |
| C3 | Synchronous macro runtime supports named placeholder bindings for earlier-step identifiers | **PARTIAL** | Named+typed binding env exists: `dsl-core/src/binding_context.rs:94-95` (`HashMap<String, BindingInfo>`), `BindingInfo{name, produced_type, ...}` (`:20-24`), `from_produces` (`:68`); lookups name-keyed only (`:112-134`), no positional dependence. bpmn-lite counterpart `PlaceholderSchema.slots` name-keyed, untyped (`dsl/plan.rs:239-253`) | No "synchronous macro **runtime**" wired to that env was located: dsl-repo macros are inert serde data (`sem_os_ontology/src/macro_def.rs:7-21`, ordered `Vec<Value>` expansion), sole consumer a governance lint (`sem_os_policy/src/gates/governance.rs:358-362`). Deciding artifact: a macro-expansion executor reading/writing `BindingContext` — none found | §0 C3 — binding-env half holds; the runtime the claim presumes is absent |
| C4 | `StartCommand.initial_payload` carries nested tagged-union envelopes without flattening | **CONFIRMED** (with constraints) | `initial_payload: String` (opaque JSON, `transition.rs:112`); copied verbatim to `domain_payload` (`engine.rs:761`); hash path encodes nested JSON recursively (`canonical.rs:1015,1059,1068`); NaN/Infinity rejected (`canonical.rs:973-974,1042-1044`; huge-exponent test `:1763`) | Constraints: JSON validity is enforced at **hash time**, not start time — `engine.rs:753-755` swallows a malformed payload (`.ok().filter(is_object)`) and stores the raw string anyway (F3); u32 length prefixes (`canonical.rs` doc `:87`); parse depth bounded by serde_json's parser limit at `canonical.rs:1111`/`engine.rs:753` (MODEL-DERIVED: that limit is 128) | §7 envelope examples carry; validation timing must be understood as deferred |
| C5 | Candle utterance matcher exposes candidate scores usable as ranking baseline without retraining | **CONFIRMED** (location + provenance caveat) | `/dev/rust/crates/ob-semantic-matcher`: `MatchResult.similarity: f32` (`types.rs:8-16`), `find_match_with_alternatives → (MatchResult, Vec<MatchResult>)` (`matcher.rs:60-64`), `semantic_search` (`matcher.rs:197`); cosine scale via pgvector `1 - (embedding <=> $1)` (`matcher.rs:205-211`), L2-normalized (`embedder.rs:158-243`); `Device::Cpu` (`embedder.rs:75`); no RNG/dropout/sampling in crate src | Not in the dsl repo (grep: candle hits there are .md only). Exact-match pinned 1.0 (`matcher.rs:256`), phonetic capped 0.95 (`:301`); ranking arithmetic executes in pgvector (DB), outside the crate. **Provenance: the containing directory is not a git repo — evidence not commit-pinnable**; crate absent from pinned ob-poc | §0 C5 — usable as claimed, but baseline pinning impossible until the code is versioned |
| C6 | Nesting-aware pairing remediation landed | **CONFIRMED** | DFS-stack pairing gone (no `gateway_pairing_dfs`; `compute_gateway_pairing` doc `lowering.rs:1231-1244`, post-doms `:967`, shared at `:54-55,249-250`); cyclicity gate `is_cyclic_directed` (`compiler/verifier.rs:84-91`, rationale `:62-83`); masking test repurposed → `test_two_nested_and_pairs_in_and_branches_now_admitted` (`verifier.rs:1700`, history in doc `:1672-1699`; old name exists only as comment text) | — | §0 C6 baseline holds |
| S1 | Verifier theorem set as listed (V-1..V-11 + specifics) | **PARTIAL** | Entry `artifact.rs:313`; V-1 `v2_verifier.rs:162-169,893-905`; V-2 `:138-181`; V-3 `:815-847`; V-4 `:460-502`; V-5 `artifact.rs:763-826`; V-7 `v2_verifier.rs:427-442,936-948`; V-8 `:405-418`; V-10 `:531-543,867-891`; V-11 `:314-381`. (a) EndTerminate/Fail exemption CONFIRMED (`:893-921`). (b) kernel rider CONFIRMED: `lib.rs:1600` (delete-all-fibres), `:1627-1631` (Retire all still-Armed via `:3511-3531`); test `lib.rs:8033` asserts `:8189-8208`. (d) V-8 = bytecode jump-direction over `&[Instr]`; cyclicity gate = IR-graph `is_cyclic_directed` (`compiler/verifier.rs:84`) — **distinct concerns, different representations/stages** (`artifact.rs:654-663`). (f) V-11 successor model counts fork targets `artifact.rs:713-717`, guard handler+fallthrough `:664-669`, arm-error `:687-692`, race arms `:728-732`, loop target+exit `:632-637` | Divergences: **(c) no `arity ≤ out-degree` weakening exists** — V-3 is purely referential; `fork_arity` (`v2_verifier.rs:191-196`) feeds only V-7 multiplicity (verified first-hand). **V-9 is retired** (`artifact.rs:405-413`) — listed-but-nonexistent. (e) V-10 is a containment *prohibition* (GuardR not inside fork) + CancelScope-targets-GuardR, not a positive dominance proof. Beyond-list checks present: guard-budget keys (`artifact.rs:348-359`), ffi/v2-ffi decls (`:326-344,371-386`), dup error-code arms (`v2_verifier.rs:699-739`), GUARD-TIMER/ARM adjacency (`:573-663,677-765`), wait_plan-empty (`:394-399`), join_plan bijection (`:400-404`), LimitMismatch (`:235-238`), stack-height merge (`artifact.rs:429-437,504-520`). **`v2_corr_sources` has NO check** (F2) | §11.2 must drop V-9, correct V-3's arity clause and V-10's phrasing, and add the unlisted checks |
| S2 | Guard trichotomy | **PARTIAL** (one location detail) | Opcodes `types.rs:394-434` (three open + three close variants); arming family {`V2GuardArmTimer` `:470`, `V2GuardArmError` `:566-569`} — no message arm; GuardN same-transition re-arm `lib.rs:4321-4411` (Rearm `:4393-4406`, exhaustion→Consume `:4396,4407-4410`); cycle tests live, none ignored (`engine/tests.rs:1221,1316,1386,1469,1632,1705`); GuardR rollback `RollbackSnapshot{domain_payload, hash, flags, join_expected, session_stack}` (`lib.rs:3853-3859`), applied `:2500-2504,2848-2852`; counters deliberately preserved (`concurrency.rs:118-122`) | Repeat count lives on `TimerRepeatSpec{interval_ms, remaining, fired_count}` on the **timer effect** (`transition.rs:423-448`), not "on the record" — `ConcurrencyRecord` has no repeat field | §S2 wording: relocate the repeat count in the model |
| S3 | Incident path end-to-end | **CONFIRMED** (one mechanism nuance) | Zero-match → `V2RouteZeroMatch` (`lowering.rs:1876`; kernel `lib.rs:1123-1139` → `fail_contract` `:4146-4178`); `Incidented` non-terminal, `is_terminal`/`is_schedulable` exhaustive no-wildcard (`types.rs:962-964,985-996,1006-1017`); `ResolveIncident` (`lib.rs:759-804`), pc unadvanced → gateway re-executes (`:4167,796-801`); unrouted BusinessRejection → Incident (`lib.rs:2754-2759,2975-3011`; exhaustive `:2767-2771`) | Postgres scheduling excludes Incidented via **literal SQL** `state = '"Running"'::jsonb AND quarantine_state IS NULL` (`store_postgres.rs:1161-1163`, verified first-hand), not a call to `is_schedulable()` (memory store does call it, `store_memory.rs:503`) — semantics duplicated, F6 | §S3 holds; note the SQL-duplication |
| S4 | Effect error-class taxonomy | **PARTIAL** | `ErrorClass` (`types.rs:1159-1164`); full `match`es exhaustive no-wildcard (`lib.rs:2767-2771,3390-3393`; `canonical.rs:827-834`); ContractViolation sole rollback-eligible (`lib.rs:2767-2771`); attempt via `Command::EffectFailed{attempt}` (`lib.rs:2603`→`:2947,2995`); retry **counter** store-side + claim-time gate (`store_postgres.rs:3219,3231-3267,3423-3472`) | (i) "no wildcard anywhere" strictly false: `transition.rs:586` `!matches!(_, Transient)` (implicit wildcard; verified first-hand). (ii) Retry **ceiling** is a hardcoded engine constant `RetryPolicy::new(1,5,1_000,60_000)` (`engine.rs:457`), not store-resident (F4). (iii) Guard budget **no longer hardcoded**: artifact-resident per-guard `v2_guard_budgets` + `default_guard_budget` (`artifact.rs:92-95`; `store_postgres.rs:356-490`, fail-closed on missing artifact `:389-395`); counter = store table `guard_failure_budget` (`:430-444`); `conservative_default() = {version:1, max_failures:5}` (`transition.rs:662-664`) survives only as fallback | §S4: the "still hardcoded (1,5)" claim is stale — rewrite against the artifact-resident mechanism |
| S5 | Parameterization / content binding | **PARTIAL** (imprecision) | No per-instance artifact overlay anywhere (grep negative; `placeholder_values` is payload-derived runtime state, `types.rs:1063-1094`); artifact hash inside hashed extent (`canonical.rs:1118`; `persistence.rs:189`); one canonical encoder for the hash domain, zero serde_json **serialization** in `state_hash` (`persistence.rs:183-191`) | "Only via `initial_payload`" is imprecise: `session_stack` also enters per-instance via StartCommand (`transition.rs:114`). serde_json **parsing** of opaque JSON persists as hash-input handling (`canonical.rs:1111,1146-1152`) — parse, not serialize | §S5 wording |
| S6 | Addr/RecordId wall | **CONFIRMED** | Distinct newtypes (`types.rs:19`; `concurrency.rs:41`), compile_fail proof (`concurrency.rs:33-39`); `V2Join{pairing: _}` ignores the static field, pops the inherited handle (`lib.rs:1801-1805,1818`, verified first-hand); Addr uses: `pc` (`types.rs:910`), `handler` (`concurrency.rs:156`), `error_routes` (`:179`) = code refs; `opened_at` (`:171`) = static-site accounting; control stack holds `RecordId` (`types.rs:918`) | — | §S6 holds |
| S7 | Transition-read-safety lint | **CONFIRMED** (with scope facts) | Script + allowlists (`check-transition-read-safety.sh:37-40,45-64`), self-test fixture inversion (`:164-177`; `scripts/fixtures/transition_read_safety_violation.rs`); blocking CI: `layering.yml:28-31`, triggers `:3-6` (PR + push main) | It enforces **routing only**: literal `.concurrency_table()` grep (`:154`) inside non-allowlisted top-level fns of the single file `bpmn-lite-kernel/src/lib.rs`; allowlisted bodies skipped (`:147`). Partially-applied pending-awareness inside a sanctioned helper is invisible; so are aliased reads and other files (F5) | §S7: state the lint's actual guarantee precisely |
| S8 | MI / lowering status | **CONFIRMED — all implemented** (no NOT-YET-IN-CODE) | MI lowering + mandatory declared max (`ir.rs:169-182`; `parser.rs:628,647`, rejection test `:1788`; `lowering.rs:827`); inclusive/dynamic-arity landed as dual-arity `V2Fork` (glue docs `types.rs:681-724`) — no separate FORK-DYN opcode; boundary timers lower to guard opcodes, `race_plan`/`boundary_map` zero live hits (comment-only: `artifact.rs:388`, `v2_verifier.rs:1515-1521`; verified first-hand); v1 `WaitAny` deleted — absent from the `Instr` enum (variant inventory verified first-hand) | "FORK-DYN" exists as a concept only; the opcode is `V2Fork` with dynamic-arity semantics. The `WaitAny` "gap" is closed by deletion (races/guards subsume it), not by an equivalent opcode | §S8: all four items are implemented; naming should follow the code |
| S9 | Frame encodes `domain_payload` inline vs hash-only | **REFUTED** (of hash-only) | `canonical.rs:1110-1120` (verified first-hand): full payload parsed (`:1111`) and encoded **inline** via `encode_canonical_json` (`:1119`), **plus** the 32-byte hash as an adjacent field (`:1120`); folded into the frame at `persistence.rs:106-125,183-191` | Frame size grows with payload size — the payload is in the hashed extent in full, hash additionally | §8.6 must assume inline payload (both frame-size and hash-domain consequences) |
| F1 | Finding: parity-gate comparand is serde_json | — | `transition.rs:1434-1436` (verified first-hand); gate `layering.yml:34-38` | `Transition::canonical_bytes` = `serde_json::to_vec`; the native/WASM byte-equality (and the name "canonical") rest on serde_json output stability, not the canonical codec. Hash domain unaffected (`state_hash` is separate) | C1 phrasing; any Designer replay-parity reasoning |
| F2 | Finding: `v2_corr_sources` unvalidated at admission | — | `artifact.rs:313-420` contains no corr check; field cloned `:207,290` (verified first-hand) | Asymmetric vs `v2_guard_budgets`/FFI tables (address+opcode-checked `:326-359`): a corr-source keyed to a wrong/non-existent address is admitted | Designer emitting correlation bindings inherits no admission-time check |
| F3 | Finding: malformed start payload silently admitted | — | `engine.rs:753-755` `.ok().filter(is_object)`; raw string stored `:761` (verified first-hand); failure surfaces at `canonical.rs:1111-1112` (`InvalidJson`) at hash time | Deferred-failure on the start path, contra the codebase's fail-closed pattern (E6-style) | §7 payload authoring: validation timing |
| F4 | Finding: retry ceiling is an engine hardcode | — | `RetryPolicy::new(1, 5, 1_000, 60_000)` at `engine.rs:457`; decision computed engine-side `:465-471` | Same "config surface" shape the guard budget just moved artifact-side; retry max-attempts has no declared/pinned source | Any Designer surface exposing retry policy |
| F5 | Finding: read-safety lint blind spots | — | Script `:154` (literal token), `:147` (allowlist skip); single-file scope (`KERNEL_FILE` `:33`) | Routing-only guarantee; incomplete pending-awareness in an allowlisted helper, aliased reads, or reads in other files pass silently | S7 guarantee statement |
| F6 | Finding: scheduling predicate duplicated in SQL | — | `store_postgres.rs:1132,1161-1163` literal `state = '"Running"'` vs `is_schedulable()` (`types.rs:1006-1017`); memory store calls the method (`store_memory.rs:503`) | A future schedulable-state addition changes the Rust method but not the SQL literal — discipline, not structure | S3 mechanism note |

## Closing block

- Reviewed HEADs: bpmn-lite `75f7a45`; dsl `a043e7f`; ob-poc `0839e87`; `/dev/rust` un-versioned (no hash — C5 evidence not pinnable).
- Baseline ancestors `a7e5bf8`/`7cace5e`/`e8e563f`/`02b1fc0`: all present. No HALT conditions hit.
- Build: 0 errors. Tests: 1048 passed / 0 failed / 0 ignored.
- Verdicts: CONFIRMED 7 (C4, C5, C6, S3, S6, S7, S8) · PARTIAL 6 (C1, C3, S1, S2, S4, S5) · REFUTED 2 (C2, S9) · NOT-YET-IN-CODE 0 · INDETERMINATE 0 (C3's runtime-existence clause recorded inside its row with the deciding artifact).
- Findings F1–F6 registered; nothing fixed, staged, or committed.
- MODEL-DERIVED statements: one, flagged in C4 (serde_json default recursion limit value).